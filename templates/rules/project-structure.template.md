<!-- epcc-doctor: allow-stale-refs -->
<!--
  프로젝트 전용 project-structure 카드의 생성 뼈대.
  epcc-init Step 7.5가 이 파일을 읽어 플레이스홀더를 전부 실제 값으로 치환한 뒤
  프로젝트의 `.claude/rules/` 아래 `project-structure.md`로 저장한다.

  원칙:
  - 프로젝트 소유 파일 — epcc-rule-version 스탬프를 넣지 않는다 (install-rules 갱신 대상 아님)
  - 기존 코드가 있으면 트리를 **실측**해서 채운다 (ls, 2~3 depth). 추측으로 채우지 않는다
  - 신규 프로젝트면 프리셋 + 토폴로지(Step 4.7)의 목표 구조로 채운다
  - 이 주석 블록과 {{안내}} 텍스트는 생성 시 전부 제거한다
  - 실전 검증 출처: easy-peasy-claudecode-devkit 모노레포에서 앱 서비스 개발로 검증된 구성
-->
---
paths:
  - "{{SOURCE_DIR}}/**"
  # 모노레포면 공유 패키지 경로 추가 (예: "packages/**"), MSA면 서비스 루트들 추가
---

# 프로젝트 구조

{{프로젝트명}}의 {{싱글레포 | 모노레포 | MSA}} 구조. 새 파일/폴더 생성 시 이 구조를 따른다.

## 전체 구조

```
{{실측 또는 목표 디렉토리 트리 — 각 주요 디렉토리에 "무엇이 여기 사는가" 한 줄 주석.
  예 (모노레포):
  project-root/                       # 루트 = 앱
  ├── packages/
  │   └── shared/                     # 공유 코드 (타입, UI, 유틸)
  │       └── src/
  │           ├── types/              # 공유 TypeScript 타입
  │           ├── ui/                 # 공용 UI 컴포넌트
  │           └── lib/                # 공통 유틸리티
  └── src/                            # 앱 전용 코드
      ├── app/                        # 라우트/페이지
      ├── components/                 # 앱 전용 컴포넌트 (도메인별 하위 폴더)
      ├── lib/                        # 비즈니스 로직·서비스·외부 연동
      ├── hooks/                      # 앱 전용 커스텀 훅
      └── stores/                     # 앱 전용 상태 스토어

  MSA면 services/(또는 apps/) 하위 서비스별 트리 + 공유 계약 위치를 명시}}
```

## import 규칙

| 대상 | import 경로 |
| ---- | ----------- |
| {{예: 공유 타입/유틸/UI (모노레포)}} | {{예: `@scope/shared` 패키지 별칭}} |
| {{예: 앱 전용 코드}} | {{예: `@/` alias (`@/components/...`) — 상대경로 금지}} |

{{MSA면 추가: 서비스 간 코드 직접 import 금지 — API 호출 또는 이벤트/계약 패키지 경유만 허용}}

## 의존 방향

{{레이어 화살표 — 예: web → application → domain → infrastructure
  역방향으로 절대 가지 않는 선만 적는다. 단순 구조면 2줄.
  단층이면 "단층 — 해당 없음"이라고 적는다. 억지로 채우지 않는다}}

`epcc-reviewer`가 역행 import를 이 절과 대조한다 — 이 절이 없으면 판정 불가로 보고된다.

## {{shared vs 앱 전용 기준 (모노레포) | 디렉토리 배치 기준 (싱글레포) | 서비스 경계 기준 (MSA)}}

| 위치 | 기준 |
| ---- | ---- |
| {{예: `packages/shared/`}} | {{예: 앱에 독립적인 공유 코드 (타입, UI, 공통 유틸)}} |
| {{예: `src/`}} | {{예: 이 앱에서만 사용하는 코드 (페이지, 도메인 컴포넌트, 서비스 로직)}} |

{{선택 규칙 — 해당하면 유지: 컴포넌트 Props는 컴포넌트 파일에 정의, 공유 타입만 공유 패키지 types/에 정의}}
