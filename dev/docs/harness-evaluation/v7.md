# 하네스(그래프) 평가 v7

- 날짜: 2026-09-12 · HEAD: 82bf4d6+워킹트리 · 플러그인: 3.28.0(태그는 c1824c6 — HEAD까지 자산 커밋 6건 미배포) · 직전 리포트(v6 · 2026-09-09) 이후 자산 커밋 **8건** · 미커밋 자산 8파일(전체 20 → 수리 후 25)
- 시점 스탬프: Claude Code 2026-09 번들 · Fable 5.1 · 2026-09-12 — P6 판단 기준 (재조사 **수행** — 30일 이내지만 자산 커밋 8건)
- doctor `--all`(수리 후): 통과 **175** · 경고 **1**(태그 뒤 자산 커밋 6건 미배포 — 이 회차가 만든 경고) · 실패 **0** · 상주 비용 **18,252바이트**(desc 7,662/12,000) · 픽스처 **45**건(정적 29 → **31** 케이스)
- doctor(수리 전): fast 52 · graph 12 · self-test 89 · consumer 16 · usage 3 · lessons 1 = 173/0/0 · 상주 18,210B
- **이 회차의 성격**: 사용자가 요청한 「소비자 입장에서 실제로 작동하는가」 검토이고, 특히 **코덱스 교차검증의 키 입력·적용·실행 경로**를 끝까지 따라갔다. 결함 12건(E-45~E-56) 중 **12건을 같은 회차에 수리**하고, 기계 판정 가능한 5건은 `--self-test` 픽스처·`_claim`으로 굳혔다. Critical 1건(E-45)은 격리 재현 3케이스로 확정했다.

## 실행 게이트

| # | 조건 | 관측 |
| --- | --- | --- |
| ① | v6 결함이 현재 파일에서 해소됐는가 | E-21~E-43 **전건 해소 유지** — `--self-test` 89/0/0 · 「미수리」였던 3.28.0 태그·병합은 **완료**(태그 c1824c6 · origin/main = HEAD). 단 태그 뒤에 자산 커밋이 다시 쌓였다 → E-52 |
| ② | 재현이 계측을 오염시키지 않는가 | `EPCC_STATE_DIR=<스크래치>` 격리 주입. 실사용 로그 불변 |
| ③ | 재현 명령이 살아있는 훅에 막히지 않는가 | 키 모양 문자열은 런타임 조립(`"sk-proj-$(printf …)"`)으로 명령 텍스트에 리터럴을 남기지 않았다 — 훅은 정상 동작(c1 케이스가 정확히 그 패턴으로 차단됨) |

## 핵심 질문의 답 — 크로스체킹 시 코덱스 키를 받아 적용하는가

**받는다고 적혀 있었고, 적용은 되지 않았다.** `skills/codex-claude-loop/SKILL.md`(수리 전)의 지시 7줄을 하나씩 실측했다.

| # | 스킬 지시 | 실측 |
| --- | --- | --- |
| 1 | `:33` `export OPENAI_API_KEY=…` "현재 세션" | Bash 호출 간 셸 상태 비보존 — 다음 호출에서 빈 값 (프로브 재현) |
| 2 | `:34` `echo 'export …' >> ~/.zshrc` "영구 저장" | **자체 `security-check.sh`가 exit 2 차단** ("OpenAI API Key 하드코딩 → .env로 옮기세요") — 스킬과 훅이 정반대 지시 |
| 3 | `:26` "환경변수를 확인" · `codex login` 언급 0 | 공식 문서(learn.chatgpt.com/docs/auth · 2026-09-12): API 키는 `printenv OPENAI_API_KEY \| codex login --with-api-key`로 **로그인해야** 적용 · 이 머신은 `auth_mode=chatgpt`(키 없음) → 스킬은 "미설정 → 건너뜀" 판정 |
| 4 | `:47` `command -v codex` | 이 머신의 `codex` 래퍼는 실행 시 ENOENT(vendor 바이너리 부재) — `command -v`는 통과 → "사용 가능" 오판. 공식 플러그인은 `codex --version` **실행**으로 판정한다 |
| 5 | `:16` "스킬 목록에 `codex-claude-loop`이 보이면 플러그인 준비 완료" | **우리 스킬**이다 — 항상 보인다 |
| 6 | description "epcc-reviewer의 cross-check 조건 충족 시" | `agents/epcc-reviewer.md`에 codex·cross-check·호출 조건 **0건** |
| 7 | `:86,142` 기본 모델 `o4-mini` | 낡음 · 읽는 코드 0 |

수리(사용자 선택: **공식 플러그인 우선 + CLI 폴백**): 준비 판정 3상태(`/codex:setup` → `codex --version && codex login status` 실행 → 판정 불가는 비차단 건너뜀) · 인증은 `codex login`(ChatGPT) 또는 `printf '%s' "<키>" | codex login --with-api-key`로만 · 키를 zshrc·.env·config에 쓰지 않음 · 대화록 잔존 고지 · 결과는 `receiving-code-review`로. **새 명령 3종을 격리 주입 → 전부 exit 0** (스킬 지시가 자기 훅에 막히지 않음을 증명).

## 축별 판정

| 축 | 측정 (미계측 항목 명시) | 판정 |
| --- | --- | --- |
| P1 라우팅·정본 일관성 | S/M/L 9건 전부 등급표(v6 동일 오탐) · G6 13건 · G9 통과 · 버전 4곳 3.28.0 · `shared-copy` 0 · `_claim` 37건 일치 — 그런데 **패턴 밖 사본 3곳이 틀려 있었다**(매뉴얼 2곳 "26개" · 해부 "수동 전용 6개"=E-49·50) → `_claim` 40건으로 확장 · **호출 계약 편측**(스킬 description ↔ 리뷰어 본문 · E-48) | **부분 충족 → 수리 후 충족** — 기계 대조가 닿는 곳은 전부 일치, 닿지 않던 3곳이 전부 낡아 있었다. 「손으로 적은 수는 반드시 낡는다」가 네 번째로 재현됐다 |
| P2 발동 정확성 | `--self-test` 양성 23종/픽스처 39 · 우회 2종 exit 2 · 무해 8종 · G8 통과 · **G8 역방향 부재**(dmi 2개가 그래프에서 자동 발동 가능으로 그려짐 · E-51) · codex 스킬의 준비 판정이 **존재 확인**(`command -v`)이라 깨진 CLI를 통과(E-46) | **부분 충족 → 수리 후 충족** — 차단력은 v6 상태 유지. 발동 조건 쪽에서 편방향 검사 1건과 오판 1건. G8' + 픽스처 `dmi-not-manual`로 봉합 |
| P3 루프 종료성 | 상한 보유 4파일(council · research · shared-workflow · superiority-patterns) · 무한 반복 선언 0 | **충족** — 현재 파일로 재확인. codex 재검증 상한 2회 유지 |
| P4 되먹임 폐쇄성 | `--lessons` 17건 · 아카이브 4 → **5** · 오탐 2건(장치별 2 미만) · `eval-report → lessons` 엣지 실재 · 이번 회차 아카이브 이동 1건(위임 측정 오류 — 수축을 스킬 Step 2 1줄로 같은 회차에 완료) | **충족** — 루프가 돌고 수축 출구가 실제로 쓰였다. 승격 0 · 수축 1 |
| P5 경로 비용 | 상주 18,210 → **18,252B**(codex desc +42B · 7,662/12,000) · 코딩 세션 41,079B · 조건부 카드 `paths:` 폭 v6 수리 유지(`harness-change` 163줄 → `scripts/**`는 여전히 소비자 자기 스크립트에 걸린다 — 소비자 없어 비용 미계측) · codex SKILL 145→157줄(호출시 7.3→9.5KB · 1회성) | **충족** — 상주 예산 안. 조건부 카드 실측은 소비자 없음으로 미계측(v6 동일) |
| P6 구조 절제 | ①과잉: codex 스킬의 **자체 인증 관리**(export·zshrc·Step 0 코드블록) — 공식 플러그인 `/codex:setup`이 설치·인증·`ready` 판정을 이미 하고 CLI 자신이 `login status`를 낸다 · `o4-mini` 선언(읽는 코드 0) ②중복: **공식 `codex` 플러그인(v1.0.4 · `/codex:review`·`adversarial-review`·Stop 리뷰 게이트) ↔ `codex-claude-loop`** 다섯 관점 판정 → **합성**(아래) · 나머지 네이티브 겹침(security-review·receiving-code-review·shortcut-ledger·codebase-survey·pr-prep) 판정 변화 없음 ③애매: 「릴리스 태그 존재」가 태그 이름만 보고 초록(E-52) — 소비자 미도달을 통과로 접었다 | **미달 → 수리 후 충족** — 과잉 1·중복 1·애매 1 전부 실물. 수축 1건 실행(합성), 검사 1건을 3상태로 |
| P7 경계 | ①유출 노드 4(codex · research · receiving-code-review · planner) — codex는 Step 0.5 동의 유지 + **키 잔존 고지 추가**(대화록) ②`dev/handoff/` 10 · 비밀정보 0 · 로그 회전 3/3 ③platform-contract 최고 19일 · **훅 timeout 계약 부재**(E-55 — 기본 600s · PreToolUse 초과는 fail-open) | **충족 → 계약 1절 추가** — 경계를 넘는 노드는 전부 사전 고지 지점이 있다. 새로 적은 사실: 게이트의 강도가 timeout 안의 속도에 의존한다 |
| P8 소비자 여정 | `--consumer` 16/16 · 스킬 이름 전수 일치 · **태그 3.28.0 존재하나 HEAD까지 자산 커밋 6건**(소비자는 그 수리를 못 받는다 · E-52) · 로컬 캐시 최고 3.27.0 · `npm test`에 `--consumer` 부재(E-54) · `package.json files[]`에 `references/`·그래프 누락(E-53 · 잠재) · **코덱스 경로: 문서를 따라간 소비자는 키를 입력하고도 리뷰를 받지 못한다**(E-45) | **미달 → 수리 후 부분 충족** — 소비자 여정 중 「첫 교차검증」이 끝까지 안 갔다. 수리 후에도 **잔존: 태그 뒤 커밋 6건 + 이 회차 수리 전부가 미배포** — 버전 인상은 사용자 결정 |

## 영역별 요약 (`doctor --inventory`)

| 영역 | 인벤토리 수치 | 이번 결함 ID | 한 줄 판정 |
| --- | --- | --- | --- |
| 스킬 | 25개 · 상주 desc 7,662B(비상주 5개) · 최대 SKILL.md 443줄(epcc-init) · codex 157줄 9,450B | E-45 · E-46 · E-47 · E-48 · E-56 | **이번 회차의 중심.** 한 스킬의 인증 지시 7줄이 전부 실물과 어긋났다 — 재작성·격리 증명 |
| 규칙 카드 | 9장 1,051줄 · 상시 1 · 조건부 8 | 없음 | 스탬프 완비(위임 보고의 「스탬프 부재」 주장은 오류 — `lessons.md:13`) |
| 훅 | 5스크립트 · 차단 23 · 픽스처 39(PreToolUse) · **timeout 6/6 선언** | E-55 | 차단력 변화 없음. timeout 사실을 계약에 적고 선언 — 새 검사는 두지 않았다 |
| 에이전트 | 2 · 109/209줄 · `disallowedTools` 2/2 | E-48 | reviewer가 codex 루프를 **제안만** 한다는 1줄 — 동의 규율과 정합 |
| 가이드 팩·이음매 | 12팩 · 이음매 3 · guide-gate self-test 55/0 · pack-smoke 18/0 · `--regress` 생략(v6 이후 guides/ 커밋 0) | 없음 | 건드리지 않음 |
| 참조·템플릿·프리셋 | 10/4/15 · 스키마 1 | E-53 | `references/`가 `files[]`에 없었다 — 마켓플레이스 clone은 무사, npm 배포는 dangling(잠재) |
| 문서 (README·docs) | 10파일 3,150줄 · `_claim` 37 → **40** | E-49 · E-50 · E-55 | 패턴 밖 사본 3곳 낡음 → 전부 `_claim` 편입 · platform-contract §5.3 신설 |
| 검사 스크립트 | doctor 2,786 → 2,836줄(+50) · scripts/*.sh 4,996줄 | E-51 · E-52 | G8' 역방향 · 릴리스 「태그 뒤 커밋」 3상태 · 픽스처 +2 |
| 그래프 | 노드 48 · 엣지 87 · manual 4 → **6** | E-51 | dmi ↔ manual 양방향 일치 |

## 결함 목록 (심각도순)

| ID | 심각도 | 파일:행 | 실패 시나리오 | 개입 지점 | 수정 방향 → **처리** |
| --- | --- | --- | --- | --- | --- |
| E-45 | **Critical**(재현 3) | `codex-claude-loop/SKILL.md:24-48` | 사용자가 키를 입력해도 ①`export` 다음 호출에서 소실(프로브: 빈 값) ②`>> ~/.zshrc` 자체 훅 exit 2 ③env var는 인증 경로가 아님(공식 문서) → `codex exec` 미인증 실패 | ② | 인증을 `codex login`에 위임 · 공식 플러그인 우선 3상태 준비 판정 · 키 미저장 · 대화록 고지. 새 명령 3종 격리 주입 exit 0. **수리·증명** |
| E-46 | Important | `SKILL.md:47` | `command -v codex` 통과, 실행은 ENOENT(이 머신 실측) → "사용 가능" 오판 후 비정상 종료 | ② | `codex --version && codex login status` **실행**으로 판정. **수리** |
| E-47 | Important | `SKILL.md:16` | 자기 스킬 이름을 공식 플러그인 설치 증거로 삼음 — 항상 참 | ② | `/codex:setup` 존재 + `ready`. **수리** |
| E-48 | Important | `SKILL.md:3` ↔ `agents/epcc-reviewer.md:152-164` | description은 "reviewer의 cross-check 조건 충족 시"라 하는데 reviewer에 그 조건·이름 0건 — 실제 진입은 수동만 | ② | description을 사용자 요청·수락으로 · reviewer 절에 「제안만, 자동 실행 없음」 2줄. **수리** |
| E-49 | Important | `docs/harness-anatomy.md:831` | 수동 전용 "6개"에 `skill-enhancer`(dmi 없음) — 같은 표 다음 행과 자기모순 | ① | 5개 + `_claim "수동 전용 수"` + 픽스처 `manual-count-drift`. **수리·증명** |
| E-50 | Important | `docs/operations-manual.md:116` · `docs/user-manual.md:204` | 26→25 정리가 `_claim` 패턴 3종만 고치고 매뉴얼 2곳 미수정 | ① | 25 + `_claim` 2건 추가. **수리·증명**(40건 일치) |
| E-51 | Important | `workflow.graph.json` ↔ `epcc-init`·`harness-evaluation` frontmatter | G8이 graph→frontmatter만 봄 — dmi:true 2개가 그래프엔 자동 발동 가능 | ② | 노드 2개 `manual:true` + G8' 역방향 검사 + 픽스처 `dmi-not-manual`. **수리·증명** |
| E-52 | Important | `doctor.sh:990` 「릴리스 태그 존재」 | 태그 이름만 확인 — 태그 뒤 자산 커밋 6건은 소비자 미도달인데 초록(판정 불가를 통과로 접음) | ① | `git rev-list --count <tag>..HEAD -- <자산 경로>` > 0이면 **경고** "버전 미인상". 지금 그 경고가 켜져 있다 — 정확히 현재 상태. **수리**(픽스처 없음 — git 이력 의존, 불확실성 지도) |
| E-53 | Suggestion | `package.json` `files[]` | `references/`·`workflow.graph.json` 누락 — npm 배포 시 data-modeling 참조 링크 전부 dangling | — | 2항목 추가. **수리** |
| E-54 | Suggestion | `package.json` `test` | 소비자 실증 `--consumer`가 테스트 밖 | — | `npm test`에 편입 · CLAUDE.md 동기. **수리** |
| E-55 | Suggestion | `hooks/hooks.json` · `docs/platform-contract.md` | timeout 미선언·계약 부재. **판정 교정**: 위임 보고의 "기본 60s"는 오류 — 공식 문서 **600s** · PreToolUse 초과는 **fail-open**(차단 없이 통과) | ③ | §5.3 신설(URL·날짜) + 훅별 10~30s 선언. 새 검사 없음. **수리** |
| E-56 | Suggestion | `SKILL.md:86,142` | `o4-mini` 기본 모델 — 낡음 · 읽는 코드 0 | — | 모델 미지정(Codex 기본값). **수리** |

※ Critical 1건(E-45)은 격리 재현 — 프로브(`export` 후 다음 호출 빈 값) · `security-check.sh` exit 2(c1) · 공식 문서 대조. 나머지는 현재 파일 grep·doctor 출력·실행 근거 — Important 이하. **위임(Explore) 보고의 결함 후보 2건은 현재 파일에서 기각**(스탬프 부재 · 60s 기본값) — 결함 목록에 올리지 않았다.

## 수축 제안 — 1건 실행 (합성)

| 대상 | 사유 | 대체 경로 | 회귀 위험 |
| --- | --- | --- | --- |
| `codex-claude-loop`의 자체 인증·설치 관리(export · zshrc 저장 · `command -v` 판정 · 기본 모델 선언) | **중복+과잉** — 공식 `codex` 플러그인(v1.0.4)이 설치·인증·`ready`를 `/codex:setup`으로 내고, CLI 자신이 `codex login status`를 낸다. 다섯 관점: 커버(플러그인 ≥ 커스텀 · 단 **동의 관문·diff 한정·재검증 상한·receiving-code-review 인계**는 플러그인에 없음) · 발동 정확성(커스텀의 판정이 깨진 CLI를 통과 — 열등) · 유지비(플랫폼이 고침) · 소비자 도달(플러그인 미설치 소비자 존재 → CLI 폴백 필요) · 모델 의존성(없음) | **합성** — 준비·인증·실행은 공식 플러그인(없으면 CLI)에, 커스텀은 동의·범위·상한·인계만 얇게. `eval-report → skill-enhancer` 경로가 아니라 이 회차에서 직접 재작성(스킬 1개 · 12줄 제거 · 표 2개 추가) | 플러그인 모드 소비자: 없음(이전에도 `/codex:*`를 안내했다). CLI 모드 소비자: `OPENAI_API_KEY`만 둔 사람은 `codex login --with-api-key`를 한 번 해야 한다 — 이전 경로는 어차피 작동하지 않았다 |

**보류(v6 계승)**: `epcc-init` 443줄 · `stack-guide-generator` 344줄 — 호출 0(`skilluse.log`) · 1회성 · 판정 근거 미변.

## 전회 대비 (v6)

- 결함: Critical **1→1** · Important **16→7** · Suggestion **8→4** (총 24→**12**) · 철회 0 · **기각 2**(위임 보고 — 결함 목록 전 단계에서)
- 신규: E-45~E-56 전부 · 해소: v6 미수리 1건(태그·병합 완료) + 이번 12건 · 잔존: **태그 뒤 자산 커밋 6건 + 이 회차 수리 미배포**(사용자 결정 — E-52 경고가 매 doctor에서 가리킨다)
- 상주 비용: 17,050(v6 보고) → 18,210(수리 전 · 에이전트 733 포함 계측 정직화 반영) → **18,252B**(+42B codex description) · 오탐 이력: 아카이브 4 → **5**
- 픽스처 45(불변) · 정적 케이스 29 → **31** · doctor 검사 fast 52→51(+`_claim` 3 · 릴리스 3상태) · graph 12→**13** · self-test 89→**91**
- **패턴**: v3 "계측기 자신" · v4 "수리 절반" · v5 "픽스처는 통과하는데 실전 경로가 다름" · v6 "판정 단위가 틀림" · **v7 "적혀 있으면 작동한다고 믿었다"** — 스킬 본문의 명령 7줄 중 3줄이 실행 환경(셸 상태 비보존 · 자기 훅 · 플랫폼 인증 모델)과 어긋났고, 그 스킬은 호출 기록 0이라 4개월간 아무도 밟지 않았다. `skilluse.log` 무호출 25개는 "정상"(소비자 없음)이었지만, **무호출 자산은 검증도 0회**라는 뜻이다
- **기계화**: G8' · `_claim` 3 · 릴리스 「태그 뒤 커밋」 · 픽스처 2. 스킬 본문 코드블록 명령이 자기 훅을 통과하는지는 **이번엔 손으로 증명**(격리 주입 3건) — 기계화 후보(불확실성 지도)

## 불확실성 지도

| 항목 | 상태 | 왜 | 닫는 조건 | 어디서 |
| --- | --- | --- | --- | --- |
| **코덱스 실제 실행**(리뷰가 돌아오는가) | **여기서 불가** | 이 머신의 `@openai/codex@0.130.0` 래퍼가 vendor 바이너리 ENOENT — 재설치 필요(하네스 결함 아님) · 소비자 없음 | `npm install -g @openai/codex` 후 `/codex-claude-loop` 실호출 → `codex login status` exit 0 → 리뷰 출력 관측 | 여기(재설치 후) · 소비자 |
| 스킬 코드블록 명령 ↔ 자기 훅 통과 | **손으로 계측 — 종결(이번 회차)** | 3종 격리 주입 exit 0. 기계화(SKILL.md 펜스의 Bash 명령을 픽스처로 자동 주입)는 미구현 | 스킬 지시가 훅에 막히는 사건 2회째에 검사로 | 여기 |
| E-52 「태그 뒤 커밋」 검사의 픽스처 | 미계측 | git 이력 의존 — `--self-test` 합성 트리는 커밋 이력이 없다 | 픽스처 트리에 `git init && commit && tag && commit` 시퀀스 | 여기 |
| 이번 수리의 소비자 도달 | **미도달** | 태그 3.28.0 뒤 자산 커밋 6건 + 워킹트리 25파일 | 버전 4곳 인상 → 커밋 → `claude plugin validate .` → `claude plugin tag --push` → main | 여기(사용자 결정) |
| T1 카드가 소비자에서 실제 **로드**되는가 | 미계측(v6 잔존) | `--consumer`는 설치까지 | 소비자 세션 관측 | 소비자 |
| `harness-change.md` `paths: scripts/**` 비용 | 미계측 | 소비자 자기 `scripts/` 편집마다 163줄 로드 — 소비자 없어 빈도 불명 | `--usage --root <소비자>` | 소비자 |
| 훅 timeout 실측(선언 10~30s 안에 끝나는가) | 미계측 | `hookrun.log`는 실행 수만 기록, 소요 시간 없음 · 이 저장소 크기에서만 체감 | 큰 저장소·긴 transcript에서 build-gate·session-brief 소요 관측 | 소비자 |
| `disable-model-invocation` 런타임 비상주 · Windows 훅 0개 · planner Write 원인 | 미검증(v6 잔존) | 플랫폼 | v6과 동일 | 플랫폼 |
| Codex 기본 모델·`-s read-only` 플래그 유효성 | 미검증 | CLI 실행 불가로 `codex exec --help` 미확인 | 재설치 후 `codex exec --help` | 여기 |

## 되먹임 (이번 회차가 lessons에 남긴 것)

- **기록 → 아카이브 이동**: `[category: false-positive] 위임 보고의 결함 후보를 그대로 결함으로 셀 뻔했다 — 2회차 연속` · `- 장치: 평가 방법 — 위임(Explore) 측정`. v6에도 Explore 보고 2건이 틀렸다 → **2회 관측 = 패턴**. 수축: `harness-evaluation/SKILL.md` Step 2에 「위임 보고의 결함 후보는 파일:행을 직접 열어 재확인한 것만 목록에 올린다」 1줄 — 같은 회차에 끝나 **`docs/lessons-archive.md`로 물리 이동**(아카이브 4 → 5)
- **평가 자신의 오탐**: 결함 목록에는 없음. 목록 **전 단계**에서 2건 기각(스탬프 부재 · 60s 기본값) — 그중 하나는 공식 문서 확인이 심각도를 바꿨다(E-55 Important → Suggestion). 「플랫폼 동작은 확인하고 주장한다」가 이번엔 지켜졌다
- **개입 근거 2줄**(harness-change 규율): (a) 막으려는 실패 — "적혀 있으면 작동한다고 믿는다"(codex 스킬 3줄 · v5 "픽스처는 통과하는데 실전 경로가 다름"과 한 뿌리) · "판정 불가를 통과로 접는다"(릴리스 태그 이름만 확인 · v6 E-27·E-37과 같은 축) (b) 위험 — doctor +50줄 · codex SKILL.md 145→157줄(+42B 상주) · hooks.json timeout 선언이 느린 환경에서 브리핑을 자를 수 있다(PreToolUse는 fail-open이라 차단력에는 영향 없음 — 그래서 security-check가 15s로 가장 짧다). 다음 회차가 `hookrun.log`의 비정상 종료 수에서 그 위험을 본다
