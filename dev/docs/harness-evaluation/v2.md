# 하네스(그래프) 평가 v2

- 날짜: 2026-08-25 · HEAD: bacaa9a+ · 플러그인: 3.18.0
- 시점 스탬프: Claude Code 2026-08 번들 · Opus 5 (1M) · 2026-08-25 — P6 판단 기준
- doctor: 통과 67 · 경고 4 · 실패 0 (--all) · 상주 비용 20,034자
- 비고: **7축 체계(P6 구조 절제 · P7 경계 신설)의 첫 실행.** P1~P5는 v1과 동일 축이므로
  그대로 비교한다. P5가 v1의 "미계측"을 벗어난 첫 회다

## 축별 판정

| 축 | 측정 | 판정 |
| --- | --- | --- |
| P1 라우팅·정본 일관성 | S/M/L 잔존분은 취약점 등급표(health-check·security-review)와 평가·skill-enhancer 자신의 지시문뿐 · **라우팅 카드 Phase 호출 13건 전부 그래프 phase와 일치**(신규 검사) · 버전 4곳 일치 | **충족** — 라우팅 문맥 위반 0건. 문서 의존성 6엣지는 이 저장소에 `dev/docs/{prd,api,…}`가 없어 해당 없음 |
| P2 발동 정확성 | ①그래프 선언 대조 통과 ②`epcc-reviewer` 읽기 전용(disallowedTools) ③양성 픽스처 6건 전량 검출, `EPCC_FX_WHY=1`로 사유 대조 ④무해 픽스처 exit 0 · 기준선 오탐 0 | **충족** — ④는 이번에 계측을 시작했고 실사용 오탐 이력은 0건(기준선) |
| P3 루프 종료성 | 상한 보유 6파일: council(재질의 1회) · shared-workflow 사본 2 · 평가 스킬(스탬프 30일) · skill-enhancer | **충족** — 무한 반복 선언 0건 |
| P4 되먹임 폐쇄성 | 저장소 4계 중 lessons·eval-report만 소비 경로 보유. insights·digital-twin은 **쓰기만 존재** | **미달** — E-01 잔존. 단 **사람이 아니라 `--graph`가 잡는다**(v1 대비 변화) |
| P5 경로 비용 | 상주 20,034자 = T0 2,425 + workflow-routing 3,390 + **스킬 description 14,219(71%)** | **부분 충족** — 미계측 탈출. 예산이 있는 곳(T0 40줄)과 비용이 몰린 곳(description)이 어긋남 → E-07 |
| P6 구조 절제 | ①무호출 31개(계측 기간 짧아 판정 보류) ②네이티브 겹침 후보 6파일 ③**조건부 instrumented 엣지 2개가 정상 상태에서 영구 미실행 경고** | **부분 충족** — ③에서 신규 결함 E-04. ②는 미계측(아래) |
| P7 경계 | ①유출 노드 4(codex·gemini 루프 · research · epcc-planner) ②`dev/handoff/` 10건 40K **회전·상한 없음**(skilluse.log는 5000줄 회전 보유), 비밀정보 스캔 0건 ③platform-contract 확인 2026-08-24(180일 이내) | **부분 충족** — E-05 · E-06 |

## 결함 목록 (심각도순)

| ID | 심각도 | 파일:행 | 실패 시나리오 | 개입 지점 | 수정 방향 |
| --- | --- | --- | --- | --- | --- |
| E-05 | Important | `skills/gemini-claude-loop/SKILL.md` | cross-check 조건이 충족된 Irreversible 작업에서 이 스킬이 호출되면 소스가 외부 모델로 나가는데, 본문 어디에도 사전 확인 요구가 없다. **같은 성격의 `codex-claude-loop`는 description에 "사용자 확인 후 호출"을 명시한다** — 같은 규범을 두 노드가 다르게 주장한다 | ② 결정 시점 | codex 쪽 문구를 정본으로 삼아 gemini 쪽에 동일 조건 기재. 전송 대상 범위(diff만인지 전체인지)도 함께 |
| E-01 | Important | `skills/insight-saver/SKILL.md` · `skills/profile-update/SKILL.md` | `dev/docs/insights/`·`dev/docs/digital-twin/`에 축적되는 판단 근거를 읽는 노드가 그래프에 없다 → N세션이 지나도 규칙·스킬 diff로 반영되지 않는다. "같은 실수를 반복하지 않는다"가 lessons 1계에서만 작동 | ④ 사후 복구 | 소비 경로 설계(`--lessons` 집계 대상 확장 또는 rule-promotion 입력 확장) 또는 저장소 폐지. **v1에서 계승 — 이번 회차에 자동 검출로 전환됨** |
| E-04 | Important | `workflow.graph.json` (instrumented 선언) · `scripts/build-gate.sh:96` | `build-gate → build`는 **게이트가 실제로 차단할 때만** 방출된다. 하네스가 정상 동작하면 영원히 방출되지 않고, `--usage`는 이를 "미실행 엣지"로 매번 경고한다. `session-start → harness-evaluation`(30일 주기)도 같다. **꺼지지 않는 경고는 무시를 학습시킨다** — v1의 E-02와 같은 병이고, E-03의 수정이 `build → build-gate`만 덮었다 | ④ 사후 복구 | `instrumented`를 "정상 경로에서 방출되는 엣지"로 좁히고, 실패·주기 조건부 엣지는 별도 표식으로 죽은 엣지 판정에서 제외 |
| E-06 | Suggestion | `scripts/handoff.sh` | `dev/handoff/`는 SessionEnd마다 파일을 남기는데 회전·상한이 없다. `skilluse.log`는 5000줄 회전을 갖는다 — 같은 성격의 잔존물인데 한쪽만 정책이 있다. 세션이 쌓이면 저장소가 단조 증가하고, 내용은 작업 컨텍스트라 잠재적으로 민감하다(현재 스캔 0건) | ④ 사후 복구 | `skilluse.log`의 회전 규약을 그대로 적용(건수 또는 일수 상한) |
| E-07 | Suggestion | `templates/operating-contract.md`(예산) 대비 `skills/*/SKILL.md` frontmatter | 상주 비용의 **71%(14,219자)가 스킬 description**인데 예산이 없다. 예산이 있는 T0(2,425자)의 5.9배다. 스킬을 한 번도 호출하지 않아도 전량이 매 세션 상주한다 | ① 시작 브리핑 | 상위 항목부터 압축(scroll-stop-builder 916자 · seo-strategy 835 · scroll-stop-prompter 727 · marketing-workflow 710 · web-asset-generator 667). 임계값은 반복 증거가 쌓인 뒤에 |

## 수축 제안

| 대상 | 사유 | 대체 경로 | 회귀 위험 |
| --- | --- | --- | --- |
| 마케팅 계열 4스킬의 description | **과잉** — 상주 비용 상위 5개 중 4개가 마케팅 파이프라인이고, 셋은 `marketing-workflow`가 오케스트레이션한다. 개별 트리거 문구가 중복 | 트리거 어휘를 `marketing-workflow`에 모으고 하위 3개는 짧은 지시로 축소 | 개별 호출(`/seo-strategy` 등) 트리거 정확도 하락 — 축소 후 호출 로그로 확인 필요 |
| `skills/security-review` vs 네이티브 `/security-review` | **중복 후보** | — | **판정 유보**: 양쪽 본문을 실제로 열어 커버 영역을 비교하지 않았다. 네이티브의 존재는 중복의 증거이지 열등의 증거가 아니다 |

## 전회 대비 (v1)

- 결함: Critical 0→0 · Important 2→3 · Suggestion 1→2
- 신규: E-04 · E-05 · E-06 · E-07 (전부 신설 축 P6·P7 또는 실측화된 P5가 검출)
- 해소: E-02(track-skill 엣지 미방출) · E-03(build-gate 조기 종료) — 재확인 완료
- 상주 비용: 미계측 → 20,034자 (기준선)
- 오탐 이력: 미계측 → 0건 (`[category: false-positive]` 신설로 계측 시작)
- **지표 변별력**: 신설 3축(P5 실측·P6·P7)이 v1이 못 보던 결함 4건을 검출. 그중 E-04는
  v1의 E-02와 같은 병이 다른 엣지에 남아 있던 것 — **부분 수정을 잡아냈다**

## 미계측 항목 (정직 보고)

- **P6-② 커스텀 vs 네이티브 실본문 비교**: 후보 6파일을 grep으로 좁혔을 뿐 양쪽 본문을
  열어 커버 영역을 대조하지 않았다. 시점 스탬프가 갱신됐으므로 30일 내 재조사 대상이 아니다 —
  **다음 회차에 본문 비교를 수행한다**
- **P5 소비자 실사용 경로 비용**: 이 저장소에는 소비자가 없다. `--usage --root <소비자>`
  경로는 준비됐으나 실행 대상이 없다. 상주 비용만 실측됐고 **경로별 비용은 여전히 미계측**
- **P6-① 무호출 31스킬의 과잉 판정**: 계측 기간이 짧아 판정하지 않는다. 장기 무호출만
  폐기 후보 (안티골 8)
- **handoff 복원 충분성 (P2-③)**: PreCompact 산출물이 실제 복원에 충분한지는 컴팩트가
  발생한 세션에서만 검증 가능
- **플러그인 배포 도달성**: 사용자 캐시가 2.0.0 — 3.18.0 재설치 후 새 세션에서
  `.claude/rules/`에 T1 카드가 실제로 설치되는지 확인 필요(v2가 4개월간 놓친 것)
