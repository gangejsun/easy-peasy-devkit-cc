# 하네스(그래프) 평가 v6

- 날짜: 2026-09-09 · HEAD: 0bfb39c+워킹트리 · 플러그인: 3.28.0(미커밋·미태그) · 직전 리포트(v5 · 2026-08-26) 이후 자산 커밋 **13건** · 미커밋 자산 **60파일**(전체 73)
- 시점 스탬프: Claude Code 2026-09 번들 · Fable 5.1 · 2026-09-09 — P6 판단 기준 (재조사 **수행** — 30일 이내지만 자산 커밋 13건 · 10버전 출하)
- doctor `--all`(수리 후): 통과 **166** · 경고 **1**(미배포 태그) · 실패 **0** · 상주 비용 **17,050바이트** · 픽스처 **43**건
- doctor(수리 전 · 세션 시작 시점): `--fast --graph` 47/1/0 · 상주 16,925B · 픽스처 14 · `--lessons` false-positive 4건 승격 경고
- **이 회차의 성격**: 사용자가 요청한 10관점 전수 검토이자, 그 10관점을 스킬에 흡수한 **8축 첫 회차**다. 결함 24건(E-21~E-44) 중 **22건을 같은 회차에 수리**하고 각 수리를 `--self-test` 픽스처로 증명했다. 미수리 2건은 사용자 결정 사항(태그·병합)이다.

## 실행 게이트

| # | 조건 | 관측 |
| --- | --- | --- |
| ① | v5 결함 10건이 현재 파일에서 해소됐는가 | **전건 해소** — E-19(접두사 벗김 `doctor.sh` `sub(/^[^:]*:/`) · E-13(`epcc_handoff_dir`) · E-20(매처 `Bash` + 픽스처) · E-14(분모 고유 스크립트) · E-15(vue 4/3/2회) · E-16(릴리스 ②단계 리허설) · E-07(12,000B 래칫) · E-17(`agents` 키 제거) · E-08·E-12(태그 6개 · origin/main 3.27.0). 계승 아님 — 현재 파일 grep으로 재확인 |
| ② | 재현이 계측을 오염시키지 않는가 | `EPCC_STATE_DIR=<스크래치>`로 격리 주입. 실사용 `graph.log`·`hookrun.log` 불변 |
| ③ | 재현 명령이 살아있는 훅에 막히지 않는가 | **막혔다** — 매트릭스 명령의 DDL 문자열이 세그먼트 판정에 걸렸다(수리 후 훅의 정상 동작). 케이스를 파일로 쓰고 `bash matrix.sh`로 실행 → 스킬 Step 2에 규율로 내장 |

## 축별 판정

| 축 | 측정 (미계측 항목 명시) | 판정 |
| --- | --- | --- |
| P1 라우팅·정본 일관성 | S/M/L 9건 전부 등급표·자기 지시문(오탐) · G6 Phase 12건 일치 · **신설** G9 역방향 통과 · 버전 4곳 3.28.0 · `shared-copy` 0 · `_claim` 확장 후 **8건 드리프트 검출**(README T1 1,080/스킬 30 · harness-change 예산 40 · anatomy tool 1·주입 0 · planner 도구 목록 2곳·reviewer 1곳) → 전부 수리, 34건 일치 · **T0:20이 없는 위치(프로젝트 CLAUDE.md)를 가리킴**(E-24) | **부분 충족 → 수리 후 충족** — 4자 대조에서 hbs↔T0 어긋남 1건(E-24)이 매 세션 최대 도달 자산에 있었다. 미계측: 소비자 프로젝트의 실제 카드 로드(설치만 증명) |
| P2 발동 정확성 | ①그래프 선언 대조 통과 ②에이전트 `disallowedTools` 2/2 ③`--self-test` 양성 4→**28종**(차단 사유 22종 전부 픽스처) ④무해 3→**8종** ⑤**우회 시도 픽스처 2종 → exit 2**(수리 전 exit 0 재현) · 판정 불가 분기 실증 · **G8 manual↔frontmatter 1건 검출**(ui-ux-design) | **미달 → 수리 후 충족** — `security-check.sh:273`의 문자열 포함 면제가 파괴 게이트까지 열었다(E-21 Critical, 재현). 픽스처 4종만 있던 22개 차단 지점 중 18개는 4개월간 미증명. 미계측: Stop 게이트의 소비자 실차단(여기선 `build-gate→build` 7회 관측 — v5 미계측 종결) |
| P3 루프 종료성 | 상한 보유 5파일(council 재질의 1회 · 평가 스킬 · shared-workflow 사본 2 · skill-enhancer refs) · 무한 반복 선언 0 | **충족** — 현재 파일로 재확인. `graph.log`만 회전 상한이 없었다(E-28 · P7로 분류) |
| P4 되먹임 폐쇄성 | `--lessons` 11건 · false-positive 4 → "승격 후보" 경고(규칙은 수축이라 함 · E-27) · 아카이브 파일 **부재**(`rule-promotion.produces`가 없는 파일을 가리킴) · `eval-report→lessons` 엣지 없음(v5 P4 지적 잔존 · E-43) | **부분 충족 → 수리 후 충족** — 루프는 돌지만 **수축 쪽 출구가 없었다**: 오탐 4건이 승격 경고로 매 세션 울리고, 아카이브가 한 번도 생기지 않았다. 수리: 장치별 수축 집계 · 엣지 추가 · `docs/lessons-archive.md` 신설(4건 이동 · lessons 11→8). 이번 회차가 첫 물리 이동이다 |
| P5 경로 비용 | 상주 17,050B(T0 3,839 · routing 5,604 · desc 7,607) · 브리핑 0.3~0.9KB · **신설** `--inventory`: 호출시 최대 stack-guide-generator 116KB(SKILL 344줄) · epcc-init SKILL **438줄**(규범 300 초과) · `harness-change.md` 252줄이 `paths: dev/docs/**`로 **PRD 편집마다** 로드(E-35) · graph.log 2,163행 무한 축적 | **부분 충족** — 상주는 예산 안(7,607/12,000). 조건부 카드의 `paths:` 폭이 비용을 결정하는데 아무 축도 안 보고 있었다(E-35 수리 → `dev/docs/harness-evaluation/**`). epcc-init 438줄은 P6-①로 이관(수동·1회성이라 상주 아님 — 다음 회차 판정). 미계측: 소비자 경로별 비용(소비자 없음) |
| P6 구조 절제 | ①과잉: 설정 필드 3종(`workflow.*`·`customResources`·`disabledSkills`) 읽는 코드 0(E-30) · 죽은 검사 UPS(E-37) · 산문 줄 수 12곳(E-31) · 완료 계획 2건 `dev/active` 잔존(E-38) ②중복: 진입 조건 표 3사본 정본 표기 없음(E-40) · 네이티브 겹침 재조사 — 판정 변화 없음(security-review·receiving-code-review·shortcut-ledger·codebase-survey·pr-prep 유지 근거 그대로) ③애매: 오탐→승격 경고(E-27) · `--help`가 구현 6줄 누출(E-36) | **미달 → 수리 후 부분 충족** — ①②③ 전부 실물이 있었다. 수축 5건 실행(아래 표). 남은 것: epcc-init 438줄·stack-guide-generator 344줄의 분해 여부(판정 보류 — 1회성 스킬) |
| P7 경계 | ①유출 노드 3(codex 루프 · research · epcc-planner) — codex `Step 0.5 외부 전송 동의` 보유 ②`dev/handoff/` 10건 회전 · 비밀정보 0 · **`graph.log` 회전 없음**(2,163행·88KB · 최대 볼륨 기록자 · E-28) ③platform-contract 최고 16일 | **부분 충족 → 수리 후 충족** — 회전 상한 추가(5000→2500) + `--self-test` 불변식. 미계측: Windows 네이티브(훅 0개 가능성 · §5.2) |
| **P8 소비자 여정** (신설) | `--consumer` 16/16 · 스킬 이름 전수: **README에 7개 부재**(council-review·dev-docs-generator·prd-reviewer·prompt-enhancer·research·service-planner·TDD — TDD는 그래프 외 인바운드 0 · E-32) · 재정의 의미론 **두 문서 모순**(GS "같은 이름 덮어쓰기" vs README "병존+다른 이름" · E-33) · GS의 하드코딩 버전 v3.27.0(E-31) · 코드블록 bare 경로 2파일(소비자에서 없는 파일 · E-34) · 3.28.0 **미태그**(소비자는 3.27.0) | **미달 → 수리 후 부분 충족** — 문서를 따라간 소비자가 실물과 어긋나는 자리 4종. 수리: README 스킬 26개 표(호출자별) + `_claim` 전수 · GS 병존 모델로 정정 · 라우팅 P4에 TDD · 코드블록 경로 변수화 + 린트. **잔존: 3.28.0 미태그·미병합** — 이 회차의 모든 수리가 소비자에게는 아직 없다 |

## 영역별 요약 (`doctor --inventory`)

| 영역 | 인벤토리 수치 | 이번 결함 ID | 한 줄 판정 |
| --- | --- | --- | --- |
| 스킬 | 26개 · 상주 desc 7,607B(비상주 6개 2,184B) · 최대 SKILL.md 438줄(epcc-init) · 호출시 최대 116KB(stack-guide-generator) · 문서 언급 0인 스킬 0(수리 후) | E-26 · E-32 · E-34 · E-42 | 발동·참조는 정상. 1회성 스킬 2개의 크기는 다음 회차 P6-① 판정 |
| 규칙 카드 | 9장 1,110줄 · 상시 1 · 조건부 8 · 참조 링크 10(data-modeling) | E-24(T0) · E-25 · E-35 · E-40 | `paths:` 폭이 비용이다 — 1건 좁혔다. 사본에 정본 표기 3곳 추가 |
| 훅 | 5스크립트 1,070줄 · 차단 지점 23 · 픽스처 37(PreToolUse) · 회전 3/3 | E-21 · E-22 · E-23 · E-28 · E-29 · E-44 | **이번 회차의 중심.** 우회 1 · 오탐 3 · 미증명 18 → 전부 픽스처로 닫음 |
| 에이전트 | 2 · 109/203줄 · `disallowedTools` 2/2 | E-31(도구 목록) · E-40 | 권한 구조 정상. 문서 사본 드리프트만 |
| 가이드 팩·이음매 | 12팩(577~2,319줄) · 이음매 3 · `--regress` 미실행(직전 이후 guides 커밋 0 — 생략 근거) · guide-gate self-test 55/0 · pack-smoke 18/0 | 없음 | supabase 팩 577줄은 설계(패턴 지식형 · seam.json이 근거) |
| 참조·템플릿·프리셋 | 10/4/15 파일 · 스키마 1 | E-30 · E-39 | 죽은 필드 삭제 · `$id` 정정 |
| 문서 (README·docs) | 8파일 2,192줄 · `_claim` 34건 · 이름 목록 8건 | E-31 · E-32 · E-33 · E-41 | 손으로 적은 수 12곳 삭제 또는 대조 편입. 두 문서 모순 1건 정정 |
| 검사 스크립트 | doctor 2,574줄(+374) · guide-gate 2,841 · pack-smoke 992 · 검사 헬퍼 3사본(의도 — CLAUDE.md 규약) | E-36 · E-37 | 검사 자신이 낸 오탐 2건(라벨 안 숫자 · 괄호)을 같은 회차에 잡았다 — 「검사 자신이 세 번째로 자기에게서 잡은 실수」 |
| 그래프 | 노드 46 · 엣지 79(+1) · 계측 엣지 7 · G1~G10 | E-26 · E-43 | 검사 3종 추가(G8·G9·G10). 스테이지 phase(P0·P3·P5·P6)는 어휘 앵커 — 카드 행 없음이 정상 |

## 결함 목록 (심각도순)

| ID | 심각도 | 파일:행 | 실패 시나리오 | 개입 지점 | 수정 방향 → **처리** |
| --- | --- | --- | --- | --- | --- |
| E-21 | **Critical** | `scripts/security-check.sh:273` | `case "$CMD" in *doctor.sh*\|*security-check.sh*) exit 0` — 명령 어디에든 그 문자열이 있으면 파괴 게이트·시크릿 스캔 전부 생략. **격리 재현**: `bash scripts/doctor.sh --help >/dev/null; psql -d app -c "DROP TABLE users"` → exit 0 · `echo doctor.sh; cat > .env.local <<EOF AKIA… EOF` → exit 0 (대조군 psql 단독 exit 2) | ③ | `_self_ref_cmd` — 첫 토큰이 `bash`이고 두 번째가 `*/doctor.sh\|*/security-check.sh`인 **단일 명령**(연결자 없음)에만, **시크릿 스캔에만** 면제. 파괴 게이트는 면제 없음. 픽스처 2종 → exit 2 · 단일 명령 → exit 0. **수리·증명** |
| E-22 | Important | `security-check.sh:174,105` | **오탐 재현**: `git commit -m "fix: drop table legacy_users"` · `gh pr create --title "…drop table…"` → exit 2 | ③ | 사용자 선택 ⓐ: `_message_cmd`(git commit/tag/notes/merge/revert/stash · gh · glab) + **세그먼트 판정**(`;` `&&` `\|\|` 개행) — `git commit -m x && psql -c "DROP TABLE t"`는 뒤 세그먼트가 막힌다. 픽스처 무해 2 · 양성 1. **수리·증명** |
| E-23 | Important | `security-check.sh:280` | 모든 `>`를 쓰기로 봄 — `rg 'AKIA…' . 2>/dev/null`(읽기 전용) exit 2 재현 | ③ | `_writes_file` — `2>/dev/null`·`>&2`·`&>/dev/null`·`->`·`>=` 제외, 파일 대상 `>`·`tee`·`dd`만. 픽스처 무해 1 · 양성 1. **수리·증명** |
| E-44 | Important | `security-check.sh:141` (`_strip_heredoc_bodies`) | 명령 **전체**에서 `; psql`을 찾아 힙독 유지 여부를 결정 → 파일에 쓰는 힙독 본문 안의 문자열(`cat > matrix.sh <<'EOF' … 'x; psql -c "DROP TABLE t"' … EOF`)이 실행으로 오인, exit 2 재현(이 회차의 재현 매트릭스 자체가 막혔다) | ③ | 판정을 **힙독을 여는 줄**로 한정(awk `feeds_db(line)`). 픽스처 무해 1. **수리·증명** — 수리 중 발견 |
| E-24 | Important | `templates/operating-contract.md:20` · `skills/epcc-migrate/SKILL.md:65,107` | T0가 "Phase는 프로젝트 `CLAUDE.md`의 라우팅 표"라 함 — 실물은 `.claude/rules/workflow-routing.md`(hbs:46이 위임). migrate는 존재하지 않는 hbs 「작업 라우팅」 절과 대조하라 지시 | ① | 3줄 정정(T0 41줄 유지). **수리** |
| E-25 | Important | `rules/harness-change.md:42` vs `doctor.sh:418` | 카드가 인용한 "예산 40줄" ≠ 검사 42 — doctor `:991`이 선례로 적은 바로 그 드리프트 | ② | `T0_BUDGET=42` 상수 + 문서 인용 `_claim`(픽스처 `t0-budget-drift`). **수리·증명** |
| E-26 | Important | `workflow.graph.json` ui-ux-design | `manual:true`인데 SKILL.md는 dmi 없이 자동 발동 description — doctor는 frontmatter만 봄 | ② | `manual` 제거(인바운드 `build→ui-ux-design` 실재) + G8(픽스처 `manual-no-dmi`). **수리·증명** |
| E-27 | Important | `doctor.sh` `--lessons` · `session-brief.sh:280` | false-positive 4건 → "승격 후보 — 룰 카드 없음" 매 실행·매 세션. 규칙(`rules/lessons.md:40`)은 수축이라 함. 4건은 장치가 다름(평가 방법 3 · 훅 1) | ④ | FP를 「수축 대상」으로 분리 + **장치별** 집계 절(`- 장치:` 줄 정본 · 없으면 추정 표시) · 브리핑도 분리. **수리** |
| E-28 | Important | `scripts/lib/common.sh:258` | `epcc_edge`만 회전 없음 — 2,163행·88KB, 최대 볼륨 | ④ | 5000→2500 회전 + `--self-test` 불변식(5,100행 주입 → ≤2,600 · 마지막 엣지 보존). **수리·증명** |
| E-29 | Important | `security-check.sh` vs `scripts/fixtures/` | 차단 지점 22종 중 픽스처 4종 — 고아 픽스처(역방향)만 검사 | ③ | 픽스처 14→**43** · 판정 불가 분기 실증(임시 git init) · 「차단 사유 ↔ 픽스처」 검사(사유 제목 추출 → POS_FX 대조). **수리·증명** — 22/22 |
| E-30 | Important | `docs/configuration.md:51-79` · `schema:323` · `epcc-init/assets/…:68` | `workflow.p0/p6.enabled`·`customResources`·`disabledSkills` — 읽는 코드 0 | ② | 선언 삭제(문서 절 → "존재하지 않는 필드" 안내로 대체 · 스키마 · init 템플릿) + 「설정 필드 ↔ 읽는 코드」 검사(픽스처 `config-ghost-field`). **수리·증명** |
| E-31 | Important | README:266·267·146 · anatomy 12곳 · GS:16 | 손으로 적은 수의 드리프트 — `_claim`은 첫 매치·머리줄만 | ② | 정책: 산문의 스크립트 줄 수 **삭제** · 구조 수치는 `_claim` 편입(T1 전 매치 · 비교표 스킬 수 · kind 표 · 주입 줄 수 · 에이전트 도구 목록) · GS 버전 문구 제거 · 상주 비용 표는 수 없이 "언제 드는가"만. **수리·증명** |
| E-32 | Important | README · docs · `rules/workflow-routing.md:59` | 스킬 7개 소비자 문서 부재 · TDD 그래프 외 인바운드 0 | ① | README 「스킬 26개 — 무엇이 부르는가」 표(호출자별) + 스킬 이름 전수 `_claim` · P4 행에 TDD 조건 + 그래프 phase P4. **수리·증명** |
| E-33 | Important | `docs/getting-started.md:112-124` vs `README.md:239` | 재정의 의미론 모순 — GS를 따르면 우선권을 얻지 못함 | ① | GS를 병존 모델로 정정(README 정본 인용). **수리** |
| E-34 | Important | `skill-enhancer/SKILL.md:43` · `harness-evaluation/SKILL.md` Step 1 | 코드블록 실행 명령의 bare 경로 — 소비자에서 없는 파일 | ② | `${CLAUDE_PLUGIN_ROOT:-.}` 변수화 + 코드블록 한정 린트(픽스처 `bare-path-in-fence`). **수리·증명** |
| E-35 | Important | `rules/harness-change.md` `paths: dev/docs/**` | PRD 1줄 편집에 252줄 하네스 카드 로드 | ② | `dev/docs/harness-evaluation/**`로 좁힘. **수리** |
| E-36 | Suggestion | `doctor.sh:2192` · `install-rules.sh:40` | `--help`에 `--all` 없음 · `sed -n '2,24p'`가 구현 6줄 누출 | — | awk 헤더 출력(`install-guide.sh:82` 패턴) + `--all`·`--inventory` 표기. **수리** |
| E-37 | Suggestion | `doctor.sh:300-308` | UserPromptSubmit 검사 — 등록된 훅 없음 · 픽스처 없음 | — | 삭제. (`:448` else-warn은 **철회** — 픽스처 트리에서 도달 가능한 정당한 4번째 상태) |
| E-38 | Suggestion | `dev/active/{graph-upgrade,stack-guide-generation}` | 3.1.0·3.2.0 대상 계획이 매 세션 「열린 작업」으로 표시 | — | 완료 확인(Phase V·G-5 산출물 실재) 후 `dev/archive/` 이동. **수리** |
| E-39 | Suggestion | `schema/epcc.config.schema.json:3` | `$id`가 존재하지 않는 URL(`gyeong/epcc-devkit/…/plugin/`) | — | init이 쓰는 URL로 정정. **수리** |
| E-40 | Suggestion | `agents/epcc-reviewer.md:91` · `epcc-planner.md:34` · `rules/reversibility.md:44` | 규범 사본에 정본 표기 없음(자가 점검 · 진입 조건 4상태 3사본) | — | 인용 표기 1줄씩. **수리** |
| E-41 | Suggestion | `docs/configuration.md:56` · `docs/harness-anatomy.md:583` | 3.27.0에서 제거된 "Gemini" 잔재 2곳 | — | 삭제(역추적 규율 — 저장소 전수 grep 0건 확인). **수리** |
| E-42 | Suggestion | `skills/harness-evaluation/SKILL.md` | "30일 이내면 P6 재조사 생략" — 14일에 10버전 출하 | — | `AND 자산 커밋 0건`(카드·해부 문서 동기). **수리** |
| E-43 | Suggestion | `workflow.graph.json` | `eval-report→lessons` 엣지 없음(v5 P4 지적 잔존) | — | 엣지 추가 + Step 3-7 되먹임 규율. **수리** |
| **미수리** | Important | `.claude-plugin/*` 3.28.0 vs 태그 `epcc-devkit--v3.27.0` · origin/main 3.27.0 | 이 회차의 수리 전부(E-21 포함)가 소비자에게 없다 | ① | 커밋 → `claude plugin validate .` + 로컬 설치 리허설 → `claude plugin tag --push` → main 병합 — **사용자 결정**(공개 저장소 행위). `doctor --fast`의 「미배포」 경고가 정확히 이 상태를 가리킨다 |

※ Critical 1건(E-21)은 격리 재현 2케이스 exit 0 → 수리 후 exit 2로 뒷받침됐다. E-22·23·44는 오탐 재현(exit 2 → 수리 후 exit 0). 나머지는 현재 파일 grep·doctor 출력 근거 — Important 이하.

## 수축 제안 — 5건 실행 (제안이 아니라 실행)

| 대상 | 사유 | 대체 경로 | 회귀 위험 |
| --- | --- | --- | --- |
| `--lessons`의 "승격 후보" 경고(false-positive) | **애매** — 규칙과 반대 방향의 경고가 매 세션 울렸다 | 「수축 대상」 표기 + 장치별 절. 브리핑은 FP 2건+만 별도 1줄 | 없음 — 이 경고를 근거로 한 행동은 전부 규칙 위반이었다 |
| 설정 필드 3종(`workflow.*`·`customResources`·`disabledSkills`) | **과잉** — 읽는 코드 0 | 선언 삭제 + 문서에 "존재하지 않는 필드" 1절. 검사가 재출현을 막는다 | 이 필드를 config에 둔 소비자가 있어도 동작 변화 0(원래 아무도 읽지 않았다) |
| 해부 문서의 스크립트 줄 수·상주 비용 수치 12곳 | **과잉** — 정보 가치 0·반드시 낡음 | `--inventory`·`--usage`가 매번 낸다. 구조 수치만 `_claim`으로 남김 | 없음 |
| doctor UserPromptSubmit 검사 | **과잉** — 등록 훅 0·픽스처 0 | 삭제. 그 이벤트를 쓰는 훅이 생기면 그때 픽스처와 함께 | 없음 |
| `dev/active/` 완료 계획 2건 · 오탐 교훈 4건 | **애매** — 끝난 것이 열린 것으로 표시 | `dev/archive/` · `docs/lessons-archive.md`(신설)로 물리 이동 | 없음 — 파일은 남는다 |

**보류 1건**: `epcc-init` SKILL.md 438줄·`stack-guide-generator` 344줄(규범 300줄) — 수동·1회성이라 상주 비용은 0. 분해가 이득인지는 호출 시 컨텍스트 압박이 관측돼야 판정한다(`skilluse.log`에 호출 0).

## 전회 대비 (v5)

- 결함: Critical **3→1** · Important **5→16** · Suggestion **2→8** (총 10→**24**) · 철회 **0**(v5는 1)
- 신규: E-21~E-44 전부 (v5 잔존 0) · 해소: v5 10건 전부 + 이번 22건 · 잔존: 미배포 1건(사용자 결정)
- 상주 비용: 17,477(v5) → 16,925(수리 전) → **17,050B**(T0 포인터 1줄·라우팅 P4 행 확장 · +125B) · 오탐 이력: 1→**4 기록 → 1 잔존**(아카이브 3 + 훅 4-in-1 항목 1 아카이브 = 4)
- 픽스처 14 → **43** · doctor 검사 수 fast 47 → 49 · self-test 16(v4) → **85** · 그래프 검사 9 → 12
- **패턴**: v3 "계측기 자신" · v4 "수리가 절반만 간 자리" · v5 "픽스처는 통과하는데 실전 경로가 다름" · **v6 "판정 단위가 틀림"** — E-21·22·23·44는 한 뿌리다(명령 전체를 한 문자열로 봄 · 면제를 문자열 포함으로 걺). 그리고 **"세는 자리가 없어서 안 세던 것"** — 요청 관점 10개 중 3개(영역별·소비자·불확실성)가 7축에 행이 없었고, 그 자리에서 P8 결함 4종이 나왔다
- **기계화**: 이번 회차가 낸 판정 중 기계로 굳은 것 — G8·G9·G10 · 「선언↔실물」 6종 · 설정 필드 · 코드블록 경로 · 차단 사유↔픽스처 · graph.log 회전 · `--inventory`. 각각 결함 주입 픽스처로 검출을 증명했다(정적 22 → **29** 케이스)

## 불확실성 지도

| 항목 | 상태 | 왜 | 닫는 조건 | 어디서 |
| --- | --- | --- | --- | --- |
| Stop 게이트 실차단 | **계측됨 — 종결** | `build-gate→build` 7회(graph.log 실사용) — v5 미계측 | — | — |
| 무호출 스킬 판정(P6-①) | **계측됨 — 종결** | E-19 수리 후 값이 참. 이 저장소에선 25개 무호출이 **정상**(소비자 작업이 없다) | 소비자 `--usage --root` | 소비자 |
| 소비자 실사용 경로 비용 | 여기서 불가 | 소비자 없음 | `--usage --root <소비자>` | 소비자 |
| T1 카드가 소비자에서 실제 **로드**되는가 | 미계측 | `--consumer`는 설치까지 증명 | 소비자 세션에서 카드 문구 인용 관측 · `epcc-init` 후 `ls .claude/rules/` 육안(CLAUDE.md 규율) | 소비자 |
| 이번 수리의 소비자 도달 | **미도달** | 3.28.0 미커밋·미태그 | 커밋 → 태그 → main | 여기(사용자 결정) |
| `disable-model-invocation` 런타임 비상주 | 미검증 | `plugin details` 추정기는 무시(실측 2026-09-03) · 런타임 계측 수단 없음 | 플랫폼 문서 재확인 또는 컨텍스트 덤프 | 플랫폼 |
| 바이트→토큰 환산 | 의도적 미계측 | 모델별 상이 — doctor가 환산 거부 | — | — |
| Windows 네이티브 훅 0개 가능성 | 미검증 | platform-contract §5.2 | Windows 실측 | 플랫폼 |
| planner에 Write가 실려 있던 원인 | 미해결 | 2026-09-07 실측 · 원인 불명 · `disallowedTools`로 우회 | 재현 · 플랫폼 문의 | 플랫폼 |
| 스킬 경로별 비용 | 구조적 미계측 | stage는 코드가 방출 못함 | — | — |
| `guide-gate --regress` | 이번 회차 생략 | 직전 리포트 이후 guides/ 커밋 0 · self-test 55/0·pack-smoke 18/0은 통과 | 팩을 건드린 회차 | 여기 |
| 세그먼트 판정의 미탐 | 미계측 | 인터프리터 `-e`(`node -e "client.query('DROP TABLE')"`)는 실행인데 첫 토큰이 DB 클라이언트가 아니라 **차단된다**(보수적) — 반대로 `_message_cmd` 면제 도구(gh/glab)의 부명령으로 SQL을 실행하는 경로는 없다고 판단했으나 픽스처는 없다 | 그런 도구가 생기면 양성 픽스처 | 여기 |

## 되먹임 (이번 회차가 lessons에 남긴 것)

- **기록**: `[category: false-positive] 커밋 메시지·stderr 폐기·파일 힙독 본문이 파괴/시크릿 게이트에 막혔고, 자기참조 면제는 게이트를 통째로 열었다` — `- 장치: scripts/security-check.sh`. 수축(면제 조건 좁힘)이 같은 회차에 끝나 **`docs/lessons-archive.md`로 이동**
- **아카이브 이동 3건**: 2026-09-08 힙독 오탐(수축: 힙독 본문 제거) · v2 E-06 · v5 E-18(수축: Step 2 「부재를 주장하는 결함은 세 가지를 먼저 한다」 내장) — lessons 11 → 8건, 아카이브 0 → 4건. **이 저장소의 첫 물리 이동이다**
- **평가 자신의 오탐**: 없음 — 단 Explore 서브에이전트 보고 중 2건이 틀렸고(truncate 오탐 주장 · `:448` 죽은 검사 주장) 직접 재현·재독으로 걸러냈다. 「위임한 측정은 인용이지 관측이 아니다」(2026-09-02 교훈)가 그대로 적용됐다
- **개입 근거 2줄**(harness-change 규율): (a) 막으려는 실패 — "세는 자리가 없어서 안 세던 것"(v1 8·10축 소멸 → v6 관점 3개) · "재현 없는 판정"(v2 E-06 · v5 E-18 · v6 E-21은 재현 없이는 Critical이 될 수 없었다) (b) 위험 — doctor +374줄·SKILL.md 146→141줄(순감)·description 377→528B(비상주). 다음 회차가 `--inventory` 검사 스크립트 행에서 이 증가를 본다
