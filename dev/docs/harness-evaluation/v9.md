# 하네스(그래프) 평가 v9

- 날짜: 2026-09-12 · HEAD: 640ae56(+워킹트리 — v8 수리분 미커밋 위에서 시작) · 플러그인: 3.29.0 · 직전 리포트(v8 · 같은 날) 이후 자산 커밋 0건 · 미커밋 자산 9 → 수리 후 **16파일**
- 시점 스탬프: Claude Code 2026-09 번들 · Fable 5.1 · 2026-09-12 — P6 재조사 생략(v8과 같은 근거)
- doctor `--all`: 통과 177 → **185** · 경고 0 · 실패 0 · 상주 비용 **18,252바이트**(불변) · 픽스처 45 → **47**건 · 정적 케이스 32 → **33**
- **이 회차의 성격**: 사용자 요청 「한번 더」. v8이 손대지 않은 곳만 봤다 — 차단 장치 둘에 **픽스처에 없는 케이스를 격리 주입**했고, 가이드·디자인 하네스 자기검증(guide-gate 55/0 · pack-smoke 18/0 · design-sync 5/5)을 돌렸고, 안 읽은 카드 둘(`ui-design`·`data-modeling`)의 `paths:` 중괄호 글롭을 플랫폼 계약 §3과 대조했다(지원 확인). **v8의 P2 「충족」 판정은 틀렸다** — 그 교정이 이 회차의 중심이다.

## 축별 판정

| 축 | 측정 (미계측 항목 명시) | 판정 |
| --- | --- | --- |
| P1 라우팅·정본 일관성 | v8과 동일(자산 커밋 0) · 중괄호 글롭 `**/*.{css,scss}` — 계약 §3 「중괄호 확장 지원」 확인 | **충족** |
| P2 발동 정확성 | **주입 결과** — security-check: `git push origin +main` · `+HEAD:main` · `--delete main` · `:main` **전부 exit 0**(우회 3형태) · build-gate: `pytest` · `pytest -q` · `python -m pytest` · `tsc --noEmit` · `jest` **미인식**(오탐 차단 5) · `echo npm run build` · `grep 'pnpm build'` **인식**(미탐 2). fastapi 프리셋 `commands.test`가 `pytest`이고 안내문도 그 명령을 찍는데 그 명령을 돌리면 막혔다 — **자기 처방을 자기 판정기가 못 알아봤다** | **미달 → 수리 후 충족** — 인식을 세그먼트 첫 토큰으로(security-check 면제와 같은 규율) · 강제 푸시 입구에 `+refspec`·원격 삭제 · 픽스처 2 + `--consumer` 5케이스 |
| P3 루프 종료성 | v8과 동일 | **충족** |
| P4 되먹임 폐쇄성 | 이번 회차 lessons **+1**(`false-negative` — 평가 자신의 미탐) · 승격: 평가 스킬 Step 2 1줄 + `--consumer` 케이스 5 | **충족** — 루프가 평가 자신에게도 돌았다 |
| P5 경로 비용 | 상주 불변 · build-gate 139 → 169줄 · security-check 500 → 521줄(호출 시 비용, 상주 0) | **충족** |
| P6 구조 절제 | 새 자산 0 · 새 검사 1(`_claim` 차단 사유 수 — 기존 헬퍼 재사용) · 경고 0 | **충족** |
| P7 경계 | v8과 동일 | **충족** |
| P8 소비자 여정 | 그림 설명서의 「막는 것 — 23종」 3곳×2언어가 24로 바뀌어야 했다 — v8이 세운 이름 집합 검사는 **수**는 안 본다 → `_claim` 추가 + 픽스처 | **충족(수리 후)** |

## 영역별 요약

| 영역 | 인벤토리 수치 | 이번 결함 ID | 한 줄 판정 |
| --- | --- | --- | --- |
| 스킬 | 25 · 상주 7,662B | 없음 | 변화 없음 |
| 규칙 카드 | 9장 · 중괄호 글롭 4건 지원 확인 | 없음 | `ui-design`·`data-modeling` 처음 열어 확인 |
| 훅 | 5 · 차단 사유 **23 → 24** · 픽스처 39 → **41**(PreToolUse) | **E-60 · E-61** | 두 게이트 모두 픽스처 밖에서 틀렸다 — 주입으로만 드러났다 |
| 에이전트 | 2 | 없음 | 변화 없음 |
| 가이드 팩·이음매 | guide-gate 55/0 · pack-smoke 18/0 (이번에 실행) | 없음 | v8은 미실행이었다 — 종결 |
| 참조·템플릿·프리셋 | fastapi `commands.test: pytest` | E-60 근거 | 처방은 옳았고 판정기가 틀렸다 |
| 문서 | 그림 설명서 한·영 「23종」 → 24 + 목록 행 1 · 운영 매뉴얼 대조 목록 +1 | E-61 | HTML은 여전히 손으로 고친다 — 수는 이제 기계가 본다 |
| 검사 스크립트 | doctor 2,866 → **2,909**줄 · `--consumer` 절 1 · `_claim` 1 · 픽스처 3(정적 1 + PreToolUse 2) | E-60 · E-61 | 양쪽(오탐·미탐)을 각각 증명 |
| 평가 스킬 | Step 2 +1줄(주입 규율) | E-62 | v8의 판정 오류를 절차로 막았다 |

## 결함 목록 (심각도순)

| ID | 심각도 | 파일:행 | 실패 시나리오 | 개입 지점 | 수정 방향 | 처리 |
| --- | --- | --- | --- | --- | --- | --- |
| E-60 | **Important** | `scripts/build-gate.sh:76-83`(구 정규식) | fastapi 프로젝트가 처방대로 `pytest`를 돌리고 소스를 고친 뒤 Stop → "빌드/테스트가 실행되지 않았습니다 · 실행하세요: pytest" **차단**. 사용자는 방금 돌린 명령을 다시 하라는 말을 듣는다 → 훅을 끈다. 반대로 `echo npm run build`·`grep 'pnpm build'`는 실행으로 인정 — v2 stop-guard의 실패가 좁아진 채 남아 있었다 | ④ | 세그먼트 첫 토큰 판정(env·wrapper 제거 · 검증기 자체 / 러너+어휘 / 언어 도구 부명령 · `install`·`add` 세그먼트 제외) · `--consumer` 5케이스(인식 3 · 비인식 2) | **수리·증명**(수리 전 프로브에서 5 미인식·2 오인식 → 수리 후 전부 교정) |
| E-61 | Important | `scripts/security-check.sh:300-322`(구) · `docs/manual/*.html` 「23종」 | 가드가 「공유 브랜치 강제 푸시 차단」을 선언하는데 `git push origin +main`(refspec 강제 푸시의 원형)이 통과 · `--delete main`·`:main`(원격 브랜치 삭제)도 통과 — E-21과 같은 부류(선언한 차단에 우회 경로) | ③ | 입구를 넷으로(`--force`·`-f`·`+refspec`·삭제), 판정 규칙(대상 브랜치 · 판정 불가 통과)은 그대로 · 삭제는 별도 사유 「공유 브랜치 원격 삭제」 · 픽스처 2 · 그림 설명서 24종 + 행 1 · `_claim` 차단 사유 수(한·영) + 픽스처 | **수리·증명**(격리 주입 8케이스 — 차단 5 · 경고 1 · 통과 2 전부 의도대로) |
| E-62 | Suggestion | `harness/skills/harness-evaluation/SKILL.md` Step 2 | v8이 두 훅을 한 줄씩 읽고 `--self-test` 초록을 근거로 P2 충족을 냈다 — 독해는 의도를, 픽스처는 아는 케이스를 증명할 뿐 「모르는 케이스가 없다」는 말하지 않는다. 절차에 「픽스처 밖을 찔러본다」가 없었다 | ② | Step 2에 1줄: P2는 주입으로만 판정 · Stop 게이트는 합성 transcript | **수리** |

※ Critical 0. E-60·E-61은 격리 주입 재현(exit 코드·차단 사유 대조)이 있으나 **실사용 관측은 0회**다 — 그래서 Important에 둔다. 두 건의 개입 근거 (a)는 「픽스처 통과 ≠ 우회 없음」의 2회 관측(v6 E-21 · 이번)이다.

## 검토했으나 개입하지 않는 것

| 대상 | 왜 개입하지 않는가 |
| --- | --- |
| `psql -f drop.sql` · `cat x.sql \| psql`류 파일 안의 파괴 구문 | 훅 자신이 「알려진 한계」로 적어둔 원리적 경계 — 정규식을 키우면 오탐이 먼저 커진다 |
| `git push -f origin feature/x` | 경고만 — 설계대로(기능 브랜치는 되돌림 가능) |
| build-gate: 파일 힙독 **본문** 한 줄이 검증기 이름으로 시작하면 실행으로 셈 | 명령 텍스트만 보는 판정의 원리적 경계 — 주석에 적었다. 힙독 본문 제거를 여기에도 넣으면 두 훅이 같은 awk를 사본으로 갖는다(공유 lib로 올릴 만큼의 관측 없음) |
| `rm -rf` · `reset --hard` 경고 수준 | v8과 동일 — 설계된 선택 |
| v8의 나머지 「개입 불요」 7건 | 근거 미변 |

## 수축 제안 (최소 1건 검토 — 제안 의무 아님)

없음 — 이번 회차는 새 자산을 만들지 않았고(검사 1·픽스처 3·스킬 1줄), 후보 재검토 결과 v8과 같다.

## 전회 대비 (v8)

- 결함: Critical 0→0 · Important **1→2** · Suggestion 2→1 (총 3→**3**) · 철회 0 · **v8 판정 교정 1**(P2 충족 → 미달이었다)
- 신규: E-60 · E-61 · E-62 전부 같은 회차 수리 · 해소: v8 E-57~59 수리 유지 · 잔존: 전부 미배포(사용자 결정)
- 상주 비용 18,252B 불변 · 오탐 이력 2 · **미탐 이력 1 신설**(`false-negative`) · 아카이브 5
- 픽스처 45 → **47** · 정적 32 → **33** · default 65 · self-test 92 → **95** · consumer 16 → **21** · `--all` 177 → **185**
- **패턴**: v8 "손으로 적은 이름은 기계가 닿는 곳에서만 살아남는다" → **v9 "읽은 것과 찔러본 것은 다르다"** — 두 게이트의 결함은 코드를 전부 읽고도 안 보였고, 픽스처 밖 케이스 13개를 넣자 8개가 틀렸다. 그중 가장 나쁜 것은 **저장소 자신의 처방(`pytest`)을 자신의 판정기가 못 알아본 것**이다 — 처방과 판정기가 같은 저장소에 4개월 있었는데 둘을 맞대본 검사가 없었다

## 불확실성 지도

| 항목 | 상태 | 왜 | 닫는 조건 | 어디서 |
| --- | --- | --- | --- | --- |
| build-gate 오탐의 **실사용 발생 여부** | 미계측 | 소비자 없음 · 이 저장소는 셸이라 `pytest`를 안 돈다 | fastapi 소비자의 `hookrun.log`·`graph.log`(build-gate→build 엣지 수) | 소비자 |
| 새 인식기의 오인식(설치 어휘 제외 규칙이 놓치는 형태) | 부분 계측 | 33케이스 프로브 + consumer 5 — 실사용 명령 분포는 모른다 | 소비자 `graph.log`에서 build-gate→build 재진입 비율 관측 | 소비자 |
| 그림 설명서 아티팩트본·다운로드본 | 여기서 불가 | v8과 동일 + 이번 24종 수정도 미반영 | `manual-variants.py` 재실행 + 재발행 | 여기(사용자) |
| 이번 수리의 소비자 도달 | 미도달 | v8·v9 수리 16파일 전부 태그 뒤 | 3.30.0 5단계 | 여기(사용자 결정) |
| v8 잔존 항목(코덱스·T1 로드·Windows·Sonar·v2 소비자·origin fetch) | 동일 | 동일 | 동일 | — |

## 되먹임 (이번 회차가 lessons에 남긴 것)

- **기록**: `[category: false-negative] 차단 장치의 코드를 전부 읽고 P2 「충족」을 냈는데, 픽스처 밖 케이스를 주입하자 우회 3건·오탐 5건이 나왔다` · `- 장치: 평가 방법 — 코드 독해로 P2 판정`. 승격은 같은 회차에 끝났다(스킬 Step 2 1줄 + consumer 케이스) — 그러나 **아카이브로 옮기지 않는다**: 이 항목은 수축이 아니라 절차 추가이고, 다음 평가가 실제로 주입을 했는지 확인할 때까지 lessons에 남긴다
- **개입 근거 2줄**: (a) 막으려는 실패 — 「픽스처 통과 ≠ 우회 없음」(v6 E-21 · v9 E-61) · 「자기 처방을 판정기가 못 알아봄」(E-60 — 관측 1이지만 프리셋 파일이 처방을 명시하므로 재현이 곧 반복이다) (b) 잘 되던 것을 망칠 위험 — build-gate 인식 확대가 **새 오인식**을 만들 수 있다(`python manage.py test`처럼 넓게 잡는다). 그래서 설치 어휘 제외를 넣고 33케이스로 경계를 그었고, 다음 회차가 소비자 `graph.log`의 재진입 비율로 본다
