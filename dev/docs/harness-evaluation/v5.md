# 하네스(그래프) 평가 v5

- 날짜: 2026-08-26 · HEAD: bacaa9a+워킹트리(미커밋 68) · 플러그인(저장소): 3.18.0
- 설치본: **3.18.0** (로컬 디렉토리 마켓플레이스) · **원격 main: 2.0.0** · 릴리스 태그 **0개**
- 시점 스탬프: Claude Code 2026-08 번들 · Opus 5 (1M) · 2026-08-26 — P6 판단 기준
- doctor `--all`: 통과 **69** · 경고 **2** · 실패 **0** · 상주 비용 **17,477자**
- **이 회차의 성격**: v1~v4는 전부 정적 자산만 봤다. v4까지 `hookrun/graph/skilluse.log`는
  비어 있거나(격리 직후) 픽스처 산출물이었다. **v5는 실사용 계측이 존재하는 첫 회차다** —
  재시작으로 3.18.0 훅 6개가 실제 세션에서 돌기 시작했다. **신규 결함 3건이 전부 그 데이터에서만 나왔다.**

## 실행 게이트 (이번 회차의 전제)

v4는 설치본이 2.0.0이라 스킬이 폐기된 10축 본문을 로드했고, 저장소 정본을 직접 읽어 우회 수행했다.
v5는 **7축 스킬이 실제로 실행한 첫 회차**다. 착수 전 4조건을 확인했다:

| # | 조건 | 관측 |
| --- | --- | --- |
| ① | 스킬 본문이 7축 | base dir = `<repo>/skills/harness-evaluation` · 본문 P1~P7 |
| ② | 2.0.0 전용 스킬 10개 소멸 | `insight-saver`·`profile-update`·`skill-generator`·`requesting-code-review`·`nextjs-*`3·`react-vite-*`2·`python-fastapi-backend-guide` 전부 부재. 29개 |
| ③ | 에이전트 교체 | `epcc-planner`·`epcc-reviewer` 등장 · `planning-agent`·`review-agent` 소멸 |
| ④ | SessionStart 출력 | "8번 + 10번 축" 소멸 → T0 운영 계약 + `session-brief` 브리핑 |

> **③이 v4의 E-17을 종결시킨다** — 배열 선언 형태에서도 에이전트는 **런타임에 정상 로드된다**.
> `claude plugin details`의 `Agents (0)`은 미로드가 아니라 **인벤토리 거짓 보고**로 확정.
>
> **디렉토리 소스 마켓플레이스는 캐시 사본이 아니라 저장소를 직접 쓴다** — `CLAUDE_PLUGIN_ROOT`가
> `/Users/gyeong/Documents/easy-peasy-devkit-cc/`(저장소)로 해석됐다. v4가 "캐시는 사본이므로
> 워킹트리 편집은 재설치 전까지 미반영"이라 적은 것은 **틀렸다**. 설치는 스테이징 사본을 만들지만
> 런타임 해석은 소스 경로다

## 축별 판정

| 축 | 측정 | 판정 |
| --- | --- | --- |
| P1 라우팅·정본 일관성 | S/M/L 9건 전부 취약점 등급표(health-check·security-review)와 자기 지시문(평가·skill-enhancer) — 오탐 · Phase 대조 13건 일치 · 버전 4곳 3.18.0 일치 · shared-copy 드리프트 0 · `_claim` 4건 일치 | **부분 충족** — v4와 동일한 2건이 그대로다: `vue` 축이 소비자 문서 3곳에 **0회**인데 실물 프리셋은 5개(E-15) · 훅 개수를 세 곳이 다른 분모로 센다(E-14). **이번엔 후자가 실물로 관측됐다** — 세션 시작 브리핑이 `훅 생존 2/6`을, 같은 세션의 doctor가 `살아있는 훅 3/5`를 출력했다 |
| P2 발동 정확성 | ①그래프 선언 대조 통과 ②`epcc-reviewer` 읽기 전용(`disallowedTools`) — **런타임 로드 확인** ③`--self-test` 훅 6/6 · 시크릿 주입 → exit 2 · 정적 양성 픽스처 7종 전량 검출 ④무해 픽스처 exit 0 · 오탐 이력 1건 · `--consumer` T1 8/8 | **부분 충족** — 등록 6개 전부 실사용에서 하트비트를 남겼다(`build-gate`는 턴 완료 시점인 10:32:04Z). 그러나 시크릿 차단 매처가 `Edit\|Write\|MultiEdit`뿐이라 **Bash 파일 쓰기는 게이트 밖**이다(E-20) — 픽스처가 차단을 증명해도 **그 도구 경로로 오지 않으면 0이다.** 커버리지의 구멍은 경로별 픽스처가 없으면 보이지 않는다 |
| P3 루프 종료성 | 상한 보유 6파일: council(재질의 1회) · gemini SKILL · 평가 스킬(스탬프 30일) · shared-workflow 사본 2 · skill-enhancer references | **충족** — 무한 반복 선언 0건. 현재 파일로 재확인 |
| P4 되먹임 폐쇄성 | `--graph`의 "모든 저장소에 소비 경로 존재" 통과(store 2) · `--lessons` 2건(`architecture` 1 · `false-positive` 1) · 승격 후보 0 | **충족** — v4에서 확인한 E-01 폐지가 유지된다. 다만 **이번 회차가 낸 결함 3건 중 어느 것도 `lessons.md`에 들어갈 경로가 자동으로 열리지 않는다** — 평가 리포트(`eval-report`)와 교훈(`lessons`)이 별개 저장소이고 둘을 잇는 엣지가 없다. 폐쇄성은 각 계 안에서만 성립 |
| P5 경로 비용 | 상주 17,477자 = T0 2,425 + workflow-routing 3,390 + description 11,662(67%). 플랫폼 자체 계산 always-on **~5,068 tok**, 훅은 0(harness-only) · 엣지 traversal 5건 · 스킬 호출 1건 | **부분 충족** — v4 대비 불변(자산 커밋 0건). 예산이 있는 곳(T0 40줄)과 비용이 몰린 곳(description)의 어긋남 그대로(E-07). **경로별 비용은 이번에도 미계측** — 실사용 엣지가 5건뿐이라 "대표 작업 1건"을 구성할 표본이 안 된다 |
| P6 구조 절제 | ①무호출 목록 — **출력은 됐으나 값이 거짓이다**(E-19) ②네이티브 겹침 — v3 스탬프 2026-08-25, 30일 이내 → **재조사 생략**(스킬 규정) ③꺼지지 않는 경고 — doctor 쪽은 해소 유지, session-brief 쪽 잔존 | **미달** — ①이 v4에서는 "미계측"이었는데 이번에 **계측이 되자마자 틀린 값을 냈다**. `doctor --usage`가 같은 화면에서 `epcc-devkit:harness-evaluation 1`을 출력하고 바로 아래 "호출 기록 없음 29개"에 `harness-evaluation`을 포함시킨다. P6-③의 "두 검사가 같은 것을 셈"의 교과서적 사례 |
| P7 경계 | ①유출 노드 4(codex·gemini 루프 · research · epcc-planner) — codex·gemini 양쪽 `Step 0.5 외부 전송 동의` 보유 ②`dev/handoff/` 10건 회전 작동 · 비밀정보 0 ③platform-contract 확인 2026-08-24(180일 이내) | **부분 충족** — ①③ 충족. ②는 이번에 **실사용 handoff가 처음 생겼고**(`20260826-102224.md`, SessionEnd 실물) 같은 회차의 `--all`이 그것을 밀어낼 뻔했다 — 회전이 픽스처 산출물에 잠식된다(E-13) |

## 결함 목록 (심각도순)

| ID | 심각도 | 파일:행 | 실패 시나리오 | 개입 지점 | 수정 방향 |
| --- | --- | --- | --- | --- | --- |
| E-19 | **Critical** | `scripts/doctor.sh:1131` (`grep -qx "$sname"`) · `scripts/track-skill.sh:26` | **무호출 스킬 판정이 항상 "전건 미호출"을 낸다.** `track-skill.sh`는 플랫폼이 주는 정규화 이름 `epcc-devkit:harness-evaluation`을 그대로 기록하고, `doctor.sh:1131`은 `skills/` 디렉토리명 `harness-evaluation`으로 **완전 일치**(`grep -qx`) 검색한다 → 영원히 불일치. **같은 실행에서 재현**: `--usage`가 `epcc-devkit:harness-evaluation 1`을 출력하고 **바로 아래** "호출 기록 없음 29개" 목록에 `harness-evaluation`을 포함시켰다. 실패 시나리오: 유지자가 이 목록을 근거로 **실사용 중인 스킬을 폐기 후보로 올린다.** README:100이 `track-skill`의 존재 이유를 "측정 없이 컬링하지 않기 위해"라고 적었는데, 그 측정이 **측정처럼 보이는 거짓값**을 낸다 — 없는 지표보다 나쁘다 | ④ 사후 복구 | 비교 전에 접두사를 벗긴다(`${name##*:}`) 또는 `track-skill.sh`가 벗겨서 기록한다. **후자를 권한다** — 로그가 플러그인 경계에 독립적이 되고 소비자 프로젝트 스킬과 같은 축에 놓인다. 수리 전까지는 이 출력을 끄는 편이 낫다(수축 제안 참조) |
| E-08 | **Critical** | `.claude-plugin/marketplace.json`(3.18.0) 대비 `origin/main`(2.0.0) · 태그 0개 | **4회째 관측.** `origin/main` = 2.0.0 · HEAD 78 커밋 앞 · 릴리스 태그 0개. 이번 회차 직전에 **로컬 디렉토리 마켓플레이스**로 우회 설치해 이 기계에서는 3.18.0이 돌지만, 제3자가 `claude plugin install epcc-devkit`을 치면 여전히 4개월 전 하네스를 받는다. doctor는 `--fast` 경고 + `--self-test` 양성 픽스처(`release-untagged`)로 **차단력까지 증명**하는데 절차가 실행되지 않는다 | ① 시작 브리핑 | `rules/harness-change.md`의 「릴리스 도달」 4단계: 커밋 → `claude plugin validate .` → `claude plugin tag --push` → **main 병합**. ④가 없으면 나머지는 무효다. 이번 회차가 **로컬 마켓플레이스 경로가 릴리스 전 리허설로 쓸 수 있음을 실증**했다 |
| E-13 | **Critical** | `scripts/handoff.sh:79` (`HD="$EPCC_ROOT/dev/handoff"`) | **2회째 독립 재현.** `epcc_state_dir()` 격리가 `common.sh`·`build-gate`·`track-skill`·`session-brief`에만 적용되고 `handoff.sh`는 빠졌다. `--all` 1회 → 신규 `20260826-102620.md` 생성, 최고참 `20260825-151029.md` **소멸**(총량 10 유지). 같은 실행에서 `.claude/.epcc/`는 무변화 — 격리는 그쪽만 성립한다. **이번 회차에 실사용 handoff가 처음 생겼다**(`20260826-102224.md`, 실제 SessionEnd) — 지금부터는 픽스처가 **실물 복원 자료를 밀어낸다.** 평가 스킬 자신이 `dev/handoff/*.md`를 「반복 증거의 출처」로 지정하므로 평가의 입력이 오염된다 | ③ 행동 체크포인트 | `handoff.sh`의 출력 루트를 `epcc_state_dir()` 계열로 돌리거나 `EPCC_HANDOFF_DIR`을 신설한다. **"상태를 쓰는 모든 경로"를 열거하는 검사가 없어서 수리가 절반만 갔다** — `grep -l 'EPCC_ROOT/dev\|EPCC_ROOT/\.claude'`를 doctor 금지 관용구에 추가하면 재발이 기계에 맡겨진다 |
| E-20 | Important | `hooks/hooks.json`의 `PreToolUse` matcher (`Edit\|Write\|MultiEdit`) · `scripts/security-check.sh:22~25` | **시크릿 차단 게이트가 Bash 파일 쓰기를 보지 못한다.** 매처가 `Edit\|Write\|MultiEdit`뿐이고 스크립트도 `.tool_input.{file_path,content,new_string}`만 읽는다 — Bash의 `.tool_input.command`는 파싱 대상이 아니다. 실패 시나리오: `cat > .env.local <<'EOF'` 힙독으로 `SUPABASE_SERVICE_ROLE_KEY=...`를 쓰면 **차단이 0**이다. 가설이 아니다 — **이 리포트 파일도 Bash 힙독으로 저장소에 쓰였고 `security-check`는 그것을 보지 못했다.** 모델이 파일을 Bash로 쓰는 운영 모드가 존재하는 한(이 세션이 그렇다) 게이트의 실효 커버리지는 도구 선택에 좌우된다 | ③ 행동 체크포인트 | 매처에 `Bash`를 추가하고 `security-check.sh`가 `.tool_input.command`도 스캔하게 한다. **판정 불가 3상태 규율을 지킨다** — 명령 문자열에서 리다이렉션 대상을 못 가르면 차단하지 않고 명시 보고한다. `rules/security.md`의 주석이 "이 카드는 정규식이 원리적으로 못 잡는 것을 담는다"고 적었는데, **못 잡는 것이 값의 종류가 아니라 도구 경로였다** |
| E-12 | Important | `README.md:22` · `docs/getting-started.md:8` | 두 문서의 `claude plugin install epcc-devkit`은 마켓플레이스 소스(`origin/main`)를 읽으므로 **v2.0.0을 설치한다**. 안내대로 따른 신규 사용자는 README가 약속한 29스킬·축 팩이 없는 하네스를 받는다 | ① 시작 브리핑 | E-08 해소가 곧 이것의 해소다. 그전까지 로컬 설치 대안(`claude plugin marketplace add <경로>`)을 병기한다 — 이번 회차가 **작동함을 실증**했다 |
| E-14 | Important | `scripts/session-brief.sh:70` | **실물로 관측됐다.** 세션 시작 브리핑이 `⚠️ 훅 생존 2/6 — 일부 훅이 실행된 적 없음`을 출력했고, 같은 세션의 `doctor --usage`는 `살아있는 훅: 3/5`를 출력했다. 분모가 6(hooks.json **엔트리 수**) vs 5(**고유 스크립트 수**)로 갈린다 — `handoff.sh`가 두 이벤트에 걸려 있어 브리핑 쪽 분자는 **구조적으로 6에 도달할 수 없다.** 부수 피해: 미달일 때마다 `epcc_edge "session-start" "doctor"`를 방출해 `graph.log`에 **거짓 traversal 2건**을 남겼다(실측) | ① 시작 브리핑 | `doctor.sh:1069`의 `jq ... \| unique \| length`를 그대로 옮긴다. 정본은 이미 존재하고(README:88 `5 스크립트 / 6 등록`) 코드만 안 따라갔다 |
| E-15 | Important | `README.md:43·90` · `docs/presets.md:11~18` · `docs/getting-started.md:29` · `scripts/doctor.sh:585~607` | `vue` 축은 실물이다(`presets/frontend/vue.json` · `guides/frontend/vue/` · `guides/seams/vue+node-api/` · `epcc-init/SKILL.md:62` 메뉴 3번). 그런데 세 소비자 문서의 `vue` 출현이 **각각 0회**이고 README:90은 `2축 4+9`로 프론트를 4개라 주장한다(실물 5). 실패 시나리오: `docs/presets.md`를 보고 Vue 프로젝트에 `react-vite`를 고르면 `install-guide.sh`가 **틀린 축 팩**을 설치하고 오선택이 이음매까지 전파된다. `_claim`은 그래프 노드·엣지·스킬 수·T1 카드 수 4건만 대조하고 **같은 표의 프리셋 행은 보지 않는다** | ① 시작 브리핑 | 문서 3곳에 `vue` 추가 + `_claim`에 프리셋 축 개수(`presets/{frontend,backend}/*.json`) 대조 추가. 축 추가 경로와 문서를 잇는 검사가 없으면 다음 축에서 같은 일이 난다 |
| E-16 | Important | `rules/harness-change.md`의 「릴리스 도달」 ② 단계 | 규칙이 `claude plugin validate .`에 "로드 거부를 설치 후에 발견하는 것을 막는다"는 역할을 부여했으나 **실측상 못 한다.** `.claude-plugin/`에 두 매니페스트가 있으면 `validate`는 **marketplace.json만** 검사한다. 합성 플러그인으로 재현: `plugin.json`에 설치기가 거부하는 `"agents": "./agents/"`를 넣어도 `validate`는 ✔를 냈고 `install`이 `Validation errors: agents: Invalid input`으로 거부했다 | ② 결정 시점 | ②단계가 `validate` **와** 로컬 마켓플레이스 경유 실제 설치를 둘 다 요구하게 한다. 이번 회차가 그 리허설 경로를 이미 만들어 뒀다 |
| E-07 | Suggestion | `templates/operating-contract.md`(예산) 대비 `skills/*/SKILL.md` frontmatter | 상주 비용의 **67%**(11,662자)가 스킬 description인데 예산이 없다. 예산이 있는 T0(2,425자)의 4.8배다. v3→v4에서 압축은 일어났으나 임계값이 없어 다음 스킬 추가 때 되돌아온다 | ① 시작 브리핑 | 플랫폼이 `claude plugin details`로 always-on 토큰을 직접 계산해 준다(~5,068 tok). 자작 문자 수 대신 이 값을 예산 단위로 삼는다 — 계산기를 만들지 않아도 된다 |
| E-17 | Suggestion | `.claude-plugin/plugin.json`의 `"agents": [...]` | **판정 확정(v4의 잠정 해소).** 재시작 후 `epcc-devkit:epcc-planner`·`epcc-reviewer`가 에이전트 목록에 실재 → **런타임 로드는 정상**이다. 그럼에도 `claude plugin details`는 `Agents (0)`을 보고한다. 합성 플러그인 3형태 격리 결과와 일치: 자동 발견 → 4/4 · 배열 선언 → 0 · 디렉토리 문자열 → 설치 거부. 즉 **미로드가 아니라 인벤토리 거짓 보고**다. 실패 시나리오: 유지자가 `details`를 신뢰해 "에이전트가 배포되지 않았다"고 오판하고 없는 결함을 고치러 간다 | ② 결정 시점 | `agents` 키를 지우고 자동 발견에 맡기면 인벤토리가 정확해진다(실측 4/4). `docs/platform-contract.md:48`이 "에이전트 키는 **기본을 대체**"라고 이미 기록하고 있어 계약 수정은 불필요하다 |

※ Critical 3건 전부 이 세션의 실행으로 재현됐다 — E-19는 `--usage` 단일 출력 내 자기모순,
E-08은 원격 대조, E-13은 `--all` 전후 `dev/handoff/` 대조. **E-18은 철회했다(아래).**

## 판정 철회 — E-18은 오탐이었다

| 항목 | 내용 |
| --- | --- |
| 철회 대상 | **E-18 (Critical) — "`build-gate`(Stop 훅)가 실사용에서 한 번도 실행되지 않았다"** |
| 반증 | `hookrun.log`에 `build-gate\|2026-08-26T10:32:04Z\|...\|0`이 있다. 세션 트랜스크립트의 Stop 요약 시각은 `10:32:04.965Z`이고 그 목록에 `scripts/build-gate.sh`가 들어 있다 — **같은 초에 발화하고 기록했다** |
| 왜 틀렸나 | `Stop`은 **도구 호출마다가 아니라 턴 완료마다** 발화한다(사용자 프롬프트 1건당 1회). 내가 로그를 읽은 10:24·10:27은 **재시작 후 첫 턴이 진행 중**이던 시점이라 그 이벤트가 발생할 기회 자체가 없었다. 빈 로그를 죽은 훅의 증거로 읽었다 |
| 통제 실험이 왜 못 막았나 | Write 1회 → `security-check` 즉시 기록은 사실이었다. 그러나 그것은 **PreToolUse가 도구마다 온다**는 것을 보였을 뿐, Stop의 발화 조건과 무관하다. **정교한 실험이 틀린 전제를 보강했다** |
| 구조적 원인 | `docs/platform-contract.md`가 이벤트별 **출력 규격**만 갖고 **언제 오는가**가 없었다. 규칙(`harness-change.md`)은 "플랫폼 동작은 확인하고 주장한다"고 요구하는데, 확인할 정본에 그 항목이 아예 없었다 |
| 재발 방지 | 계약에 **§5.1 발화 빈도**를 신설(출처·확인일 2026-08-26). `docs/lessons.md`에 `false-positive`로 기록 — 이 카테고리의 2번째 실물이다 |

> 앞선 오탐(v2의 E-06)은 "파일 개수를 보고 코드를 열지 않았다"였고, 이번은
> "**이벤트가 일어날 시간을 주지 않았다**"다. 둘 다 관측 1회로 부재를 결론냈다.
> 평가 스킬의 "실패 시나리오를 쓸 수 없으면 보고하지 않는다"는 규율은 이번에도 막지 못했다 —
> 시나리오는 쓸 수 있었고, 다만 틀렸다.

## 수리 반영 (2026-08-26, 같은 날 후속 세션)

리포트는 백로그다. 아래는 그 백로그를 실제로 소진한 기록이다 — **각 항목은 수리 후
차단·통과를 실행으로 증명했다**(그럴듯한 수정과 효과 있는 수정을 가르는 것은 재실행뿐이다).

| ID | 수리 | 증명 |
| --- | --- | --- |
| E-19 | `doctor.sh`의 표시·대조 양쪽에서 `sub(/^[^:]*:/,"",n)`로 접두사를 벗긴다 | `--usage`의 미호출 목록 29 → **28**, 방금 호출한 `harness-evaluation`이 빠졌다 |
| E-13 | `common.sh`에 `epcc_handoff_dir()` 신설 · `handoff.sh`가 그것을 쓴다 · `--self-test`가 `EPCC_HANDOFF_DIR`을 임시 디렉토리로 export | `--self-test` 전후 `dev/handoff/` **10건·최고참 불변**. 신설 불변식 검사 `픽스처가 실사용 산출물을 건드리지 않음` 추가 후 **결함 재주입 → exit 1** 확인 |
| E-14 | `session-brief.sh:70`의 분모를 `doctor.sh`와 같은 고유 스크립트 수 식으로 교체 | 실행 결과 `훅 5/5 정상` — 구조적 미달 경고 소멸 |
| E-20 | `hooks.json` 매처에 `Bash` 추가 · `security-check.sh`가 `.tool_input.command`도 스캔(**파일을 쓰는 명령만** — 읽기 명령 오탐 방지) · 양성/무해 픽스처 2종 신설 | `Bash 힙독 시크릿 주입 → exit 2` · `읽기 전용 조사 명령 → exit 0` 둘 다 `--self-test`가 증명 |
| E-17 | `plugin.json`의 `agents` 배열 선언 제거 → 자동 발견 | `claude plugin details`가 `Agents (0)` → **`Agents (2) epcc-reviewer, epcc-planner`** |
| E-15 | `vue`를 README(2곳)·`docs/presets.md`·`docs/getting-started.md`(2곳)에 추가 · `_claim`에 프리셋 축 개수 대조 신설 | 「문서 수치 주장」 4건 → **6건** 일치. `4+9` 재주입 → **exit 1** 확인 |
| E-16 | `harness-change.md`의 릴리스 ②단계를 `validate` **+ 로컬 설치 리허설**로 확장, `details` 인벤토리 대조까지 규정 | 규칙 본문에 실측 근거(`agents: Invalid input`) 기재 |
| E-07 | `--usage`에 description 예산 **12,000자 래칫** 신설 (차단 아닌 경고) | `스킬 description 11662/12000자`. 3회 연속 관측(v3·v4·v5)이 임계값의 근거다 |
| **E-18** | 철회 — 수리 대상이 아니었다. 대신 `platform-contract.md` **§5.1 발화 빈도** 신설(출처·확인일)과 `lessons.md` 오탐 기록 | 같은 오독이 재발하면 이제 계약에 걸린다 |

**미수리 2건**: `E-08`·`E-12`(미배포). 커밋 → `claude plugin tag --push` → **main 병합**이
필요하고, 이는 공개 저장소로 나가는 되돌리기 어려운 행위라 사용자 결정에 남긴다.
그전까지 `doctor --fast`의 「미배포」 경고가 상태를 정확히 지목하고 있다.

## v4 판정 처리 (계승 아님 — 전부 현재 파일·현재 실행으로 재확인)

| ID | 처리 | 근거 |
| --- | --- | --- |
| E-17 에이전트 인벤토리 | **잠정 → 확정** | 게이트 ③이 런타임 로드를 증명. 인벤토리 거짓 보고로 결론. **v4가 남긴 "잠정 유지는 실패다"라는 종결 조건을 충족** |
| E-08 · E-12 · E-13 · E-14 · E-15 · E-16 · E-07 | **전건 잔존** | 자산 커밋 0건이므로 예상대로다. 다만 E-13·E-14는 이번에 **실물 근거가 추가**됐다(재현 2회째 / 브리핑 출력 관측) |
| v4의 미계측 7건 | **4건 종결 · 3건 유지** | 종결: 종결 조건 ②③(게이트 ①④) · E-17 런타임 · P5 실사용 계측 개시. 유지: 공개 배포 도달 · 소비자 경로 비용 · handoff 복원 충분성 |

## 수축 제안

| 대상 | 사유 (과잉 or 중복 or 애매) | 대체 경로 | 회귀 위험 |
| --- | --- | --- | --- |
| `doctor --usage`의 **"호출 기록 없음 N개" 출력** | **애매** — 값이 거짓이다(E-19). "계측 기간이 짧으면 정상"이라는 완충 문구가 붙어 있어 **틀렸다는 사실 자체가 가려진다**. v3가 세운 원칙 "오염된 지표는 없는 지표보다 나쁘다"가 그대로 적용된다 | E-19를 고치면 유지, 못 고치면 출력을 끈다. 고치는 쪽이 3줄이므로 **폐지가 아니라 수리가 답인 드문 경우** | 없음 — 지금 이 출력을 근거로 한 행동은 전부 오판이다 |
| `scripts/session-brief.sh:66~84`의 **훅 생존 지표** | **중복 + 애매** — `doctor --usage`가 같은 것을 세고 **분모가 다르다**. 이번 회차에 두 값(2/6 · 3/5)이 같은 세션에서 동시에 출력됐다 | ①분모만 doctor 식으로 고쳐 남긴다(권장) ②또는 폐지하고 브리핑은 "비정상 종료 훅"만 보고 | ②는 위험하다 — **E-18이 정확히 "훅이 안 도는데 아무도 모르는" 사례**이고, 브리핑의 이 줄이 소비자가 그것을 볼 수 있는 유일한 자리다. 지표를 고쳐서 남기는 것이 옳다 |
| `.claude/.epcc/*.pre-isolation` 4파일 (57KB) | **과잉** — 실사용 로그가 생긴 이상 이 오염 데이터는 계측에 쓰이지 않는다. 읽는 코드 0건(grep) | v3·v4 근거 보존이 목적이면 `dev/docs/harness-evaluation/`로 옮겨 출처를 명시, 아니면 삭제 | 없음 |

## 전회 대비 (v4)

- 결함: Critical **2→3** · Important **4→5** · Suggestion **2→2** (총 8→10) · **철회 1건(E-18)**
- 신규: **E-19 · E-20** — **둘 다 실사용 계측이 처음 존재해서 나왔다.**
  v1~v4는 정적 자산만 봤고, 그동안 둘 다 "통과"로 보였다
- 오탐: **E-18** — 같은 계측이 낸 세 번째 결함이 오탐이었다. **새 데이터원은 새 결함과
  새 오탐을 함께 가져온다** — 계측이 생겼다고 판정 규율이 느슨해져도 되는 것이 아니다
- 해소: **없음** (v4 이후 자산 커밋 0건). E-17은 해소가 아니라 **판정 확정**
- 상주 비용: 17,477자 → 17,477자 (변동 없음)
- 오탐 이력: 1건 → 1건 (신규 없음)
- **패턴**: v3의 신규 5건은 "계측기 자신", v4의 신규 5건 중 3건은 "수리가 절반만 간 자리",
  v5의 신규 2건은 **"픽스처는 통과하는데 실전 경로가 다른 것"**이다. 세 회차가 각각
  다른 층을 벗겼고, 이번 층은 **재시작 없이는 볼 수 없었다** — 평가 도구가 아니라
  평가의 *조건*이 결정적이었다는 뜻이다
- **기계화 후퇴 없음**: doctor `--all` 69/2/0. 경고 2는 「미배포」(E-08 지목·정확)와
  「계측 엣지 중 미실행 2개」다. 후자는 **결함이 아니라 아직 안 일어난 것**이었다(E-18 철회) —
  경고는 정확했고 **해석이 틀렸다**. 「주장이 아니라 측정이다」라는 문구가 측정값의
  *의미*까지 보증하지는 않는다

## 미계측 항목 (정직 보고)

- **Stop 게이트의 실효 차단력**: `build-gate`가 발화하는 것은 확인했으나(10:32:04Z),
  실제로 **차단한 사례는 0건**이다. 차단 경로(`build-gate → build` 엣지)는 조건부라
  게이트가 막을 때만 방출된다 — 소스를 고치고 검증 없이 끝내는 턴이 발생해야 관측된다
- **무호출 스킬 판정 (P6-①)**: E-19 수리 전까지 원리적으로 불가능하다. 기간 문제가 아니다
- **경로별 비용 (P5)**: 실사용 엣지 5건뿐이라 "대표 작업 1건"의 노드·게이트 수를 구성할 표본이 없다.
  상주 비용만 실측됐다
- **공개 배포 도달**: `origin/main`이 2.0.0인 한 로컬 설치로는 증명되지 않는다.
  이번 회차가 증명한 것은 **로컬 디렉토리 마켓플레이스 경로가 작동한다**는 것뿐이다
- **handoff 복원 충분성 (P2-③)**: 실사용 handoff가 처음 생겼으나(`20260826-102224.md`)
  실제 컴팩트 복원에 충분한지는 컴팩트가 발생한 세션에서만 검증된다
- **소비자 실사용 경로 비용**: 이 저장소에는 소비자가 없다. `--usage --root <소비자>`는
  준비됐으나 실행 대상이 없다
