# 하네스(그래프) 평가 v3

- 날짜: 2026-08-25 · HEAD: bacaa9a+워킹트리 · 플러그인(저장소): 3.18.0 · **설치본(캐시): 2.0.0 → E-08**
- 시점 스탬프: Claude Code 2026-08 번들 · Opus 5 (1M) · 2026-08-25 — P6 판단 기준
- doctor: 통과 67 · 경고 4 · 실패 0 (--all) · 상주 비용 20,034자
  — **"통과 67"에는 유령 1건이 섞여 있다** (E-10)
- 비고: v2와 같은 날, 같은 커밋 위에서 수행했다. 자산이 안 변했으므로 이번 회차의 값어치는
  **v2가 미룬 것(P6-② 본문 비교)과 v2가 틀린 것(E-06)**, 그리고 **평가의 근거 데이터 자체를
  의심한 것**(E-09)에 있다. Critical 2건은 이 세션 안에서 재현됐다

## 축별 판정

| 축 | 측정 | 판정 |
| --- | --- | --- |
| P1 라우팅·정본 일관성 | S/M/L 잔존분은 취약점 등급표(health-check·security-review)와 평가·skill-enhancer의 자기 지시문뿐 · 라우팅 카드 Phase 호출 13건 전부 그래프 phase와 일치 · 버전 4곳 일치(3.18.0) · shared-copy 드리프트 0 | **부분 충족** — 저장소 안에서는 충족이나 **저장소 정본과 소비자 실물이 다르다**: 설치본 2.0.0의 `harness-evaluation`은 폐기된 10축을 주장하고, `origin/main`도 v2.0.0이다. 정본 일관성이 **저장소 경계 안에서만 성립** → E-08 · E-12 |
| P2 발동 정확성 | ①그래프 선언 대조 통과 ②`epcc-reviewer` 읽기 전용 ③양성 픽스처 6건 전량 검출(`EPCC_FX_WHY=1` 사유 대조) · security-check 시크릿 주입 → exit 2 ④무해 픽스처 exit 0 | **부분 충족** — ①②③④ 모두 통과하나, ④의 **실사용 오탐 이력이 0건이 아니었다**: v2의 E-06이 오탐이었다(아래 철회). 오탐은 훅이 아니라 **평가 노드**에서 나왔다 |
| P3 루프 종료성 | 상한 보유 6파일: council(재질의 1회) · shared-workflow 사본 2 · 평가 스킬(스탬프 30일) · skill-enhancer(references) | **충족** — 무한 반복 선언 0건. v2와 동일, 재확인함 |
| P4 되먹임 폐쇄성 | 저장소 4계 중 lessons·eval-report만 소비 경로 보유. insights·digital-twin은 쓰기만 존재(`--graph` 경고 2건) · `--lessons` 총 1건(architecture) | **미달** — E-01 잔존. 다만 이번 회차가 **오탐 1건을 산출**했으므로 `false-positive` 카테고리에 첫 실물이 생긴다 (P2-④의 입력이 비어 있던 문제 해소) |
| P5 경로 비용 | 상주 20,034자 = T0 2,425 + workflow-routing 3,390 + 스킬 description 14,219(71%) — v2와 동일 | **부분 충족** — 상주 비용은 정적 계산이라 E-09의 오염을 받지 않는다. 예산이 있는 곳(T0 40줄)과 비용이 몰린 곳(description)의 어긋남은 그대로 → E-07 |
| P6 구조 절제 | ①무호출 31개 ②네이티브 겹침 후보 3파일 **본문 대조 완료 — 중복 아님** ③조건부 instrumented 엣지 2개 영구 미실행 경고 + **`살아있는 훅 5/6`의 영구 미달** + **`비정상 종료` 2건/없음 동시 출력** | **부분 충족** — ②는 v2의 유보를 이번에 해소(중복 없음). ③에서 신규 2건(E-10·E-11). **①은 판정 불가로 강등** — 계측 자체가 오염됐다(E-09), 기간 문제가 아니다 |
| P7 경계 | ①유출 노드 4(codex·gemini 루프 · research · epcc-planner) — gemini만 사전 확인 문구 없음 ②`dev/handoff/` 10건 40K — **회전 있음**(handoff.sh:84, 최근 10개 유지), 비밀정보 스캔 0건 ③platform-contract 확인 2026-08-24(180일 이내) | **부분 충족** — ②는 **충족으로 정정**(v2 오판). ①의 E-05만 잔존 |

## 결함 목록 (심각도순)

| ID | 심각도 | 파일:행 | 실패 시나리오 | 개입 지점 | 수정 방향 |
| --- | --- | --- | --- | --- | --- |
| E-08 | **Critical** | `.claude-plugin/marketplace.json`(v3.18.0) 대비 `origin/main`(v2.0.0) · `scripts/doctor.sh`(`--consumer` 블록 927~1016) | 실제 설치본은 `~/.claude/plugins/cache/easy-peasy-devkit/epcc-devkit/**2.0.0**`인데 저장소는 3.18.0이다. **이번 세션에서 재현**: `/harness-evaluation` 호출이 2.0.0의 **폐기된 10축** SKILL.md를 로드했고(base dir이 캐시 경로), SessionStart 훅은 저장소에 존재하지도 않는 `session-start-validator.sh`를 실행해 "8번 + 10번 축" 권고를 냈다. **근본 원인은 캐시 노후가 아니라 배포 부재다**: 마켓플레이스 소스는 GitHub 원격(`known_marketplaces.json`)이고 `origin/main`은 **`36f893d`(v2.0.0)** — 이 sha가 `installed_plugins.json`의 `gitCommitSha`와 정확히 같다. 3.18.0은 로컬 `v3-harness-modernization`에만 있다(자기 원격보다 **69 커밋 앞**, 미커밋 50건). **`/plugin update`를 눌러도 v2.0.0을 다시 받는다.** `--consumer`는 저장소 자산을 합성 캐시에 복사해 검사하므로 이 스큐를 구조적으로 볼 수 없다 — 레이아웃은 증명하되 **배포는 증명하지 않는다** | ① 시작 브리핑 | 캐시 대조는 증상만 잡는다. **커밋 → 푸시 → main 병합 → 캐시 갱신** 4단계를 릴리스 절차로 명문화하고(`rules/harness-change.md`의 버전 4곳 규약 뒤), doctor에 `origin/main`의 `marketplace.json` 버전 대조를 넣는다. 검증 전까지는 **로컬 마켓플레이스 등록**(`/plugin marketplace add <저장소 경로>`)으로 푸시 없이 실물 설치해 도달을 실측한다 |
| E-09 | **Critical** | `scripts/doctor.sh:687` (`CLAUDE_PROJECT_DIR="$ROOT"`) | `--self-test`가 훅 픽스처를 **저장소 자신을 프로젝트로 지정해** 주입한다 → `.claude/.epcc/hookrun.log`·`graph.log`에 실제로 기록된다. **재현**: `--all` 1회로 hookrun 6행·graph 5행 추가(15:23:34~35Z). 그런데 이 저장소의 설치본은 2.0.0이고 그쪽 스크립트 이름은 전부 다르다(`session-start-validator.sh` 등) → **로그에 있는 `session-brief`/`build-gate`/`handoff` 기록은 실사용이 아니라 전량 doctor 자신의 픽스처 흔적**이다. `--usage`의 하트비트·엣지 traversal·미실행 엣지 판정이 모두 이 위에 서 있고, v2는 그 숫자로 P5·P6을 판정했다. 계측기가 자기가 재는 데이터를 쓴다 | ③ 행동 체크포인트 | self-test는 `CLAUDE_PROJECT_DIR`을 mktemp 디렉토리로 지정한다(픽스처 격리는 이미 `$T`로 하고 있다 — 로그만 새고 있다). 격리 후 기존 로그는 폐기하고 기준선을 다시 잡는다 |
| E-12 | Important | `README.md:22` · `docs/getting-started.md:8` | 두 문서가 안내하는 설치 명령 `claude plugin install epcc-devkit`은 **v2.0.0을 설치한다**(마켓플레이스 소스 = `origin/main` = `36f893d`). 즉 **문서가 존재하지 않는 배포를 전제로 한다** — 안내대로 따른 신규 사용자는 4개월 전 하네스를 받고, README가 약속한 "31스킬 + 축 가이드 팩"은 그 안에 없다. 배포 절차는 어디에도 없다: `.github/` 없음, `package.json`에 release 스크립트 없음, `CLAUDE.md`의 "버전 4곳 동시 인상"이 대조하는 4곳이 **전부 로컬 파일**이다. E-08과 뿌리가 같고, 이쪽은 **문서가 사실이 아니게 되는 면**이다 | ① 시작 브리핑 | 버전 인상 규약에 "④ 푸시·main 병합까지가 인상이다"를 추가한다. **플랫폼이 이미 제공하는 단계를 쓰지 않고 있다**: `claude plugin tag`는 `{name}--v{version}` 릴리스 태그를 만들면서 `plugin.json`과 마켓플레이스 엔트리의 일치를 검증하고, `claude plugin validate <path>`는 매니페스트를 검사한다. **저장소의 태그 수는 0이고**, `docs/platform-contract.md`·`rules/harness-change.md`는 두 명령을 한 번도 언급하지 않는다 — 저장소가 아는 플랫폼 명령은 `README.md:22`의 `claude plugin install` 하나뿐이다. doctor는 로컬 4곳만 보므로 **원격 대조 없이는 이 결함이 매번 재발한다** |
| E-05 | Important | `skills/gemini-claude-loop/SKILL.md:3` | cross-check 조건이 충족된 Irreversible 작업에서 이 스킬이 호출되면 소스가 외부 모델로 나가는데 본문 어디에도 사전 확인 요구가 없다. **같은 성격의 `codex-claude-loop`는 description에 "사용자 확인 후 호출"을 명시한다** — 같은 규범을 두 노드가 다르게 주장한다. 현재 파일로 재확인: 여전히 gemini 쪽에만 없다 | ② 결정 시점 | codex 쪽 문구를 정본으로 삼아 gemini에 동일 조건 기재. 전송 대상 범위(diff만인지 전체인지)도 함께 |
| E-01 | Important | `skills/insight-saver/SKILL.md` · `skills/profile-update/SKILL.md` | `dev/docs/insights/`·`dev/docs/digital-twin/`에 축적되는 판단 근거를 읽는 노드가 그래프에 없다 → N세션이 지나도 규칙·스킬 diff로 반영되지 않는다. "같은 실수를 반복하지 않는다"가 lessons 1계에서만 작동한다. v1→v2→v3 3회 연속 잔존 | ④ 사후 복구 | 소비 경로 설계(`--lessons` 집계 대상 확장 또는 rule-promotion 입력 확장) 또는 저장소 폐지. **3회 연속 잔존은 "고칠 생각이 없다"는 신호다 — 폐지 쪽을 먼저 검토한다** |
| E-04 | Important | `workflow.graph.json`(instrumented 선언) · `scripts/build-gate.sh:96` | `build-gate → build`는 게이트가 실제로 차단할 때만 방출된다. 하네스가 정상 동작하면 영원히 방출되지 않고 `--usage`는 매번 "미실행 엣지"로 경고한다. `session-start → harness-evaluation`(30일 주기)도 같다. 꺼지지 않는 경고는 무시를 학습시킨다 | ④ 사후 복구 | `instrumented`를 "정상 경로에서 방출되는 엣지"로 좁히고, 실패·주기 조건부 엣지는 별도 표식으로 죽은 엣지 판정에서 제외 |
| E-10 | Important | `scripts/doctor.sh:1034` | `[ "$(num "$fails")" -gt 0 ] && warn "비정상 종료 ${fails}건" \|\| ok "비정상 종료 없음"` — `warn`은 `$2`가 비면 반환값이 1이다(**CLAUDE.md가 명시한 함정**). 따라서 실패가 **있을 때** warn과 ok가 **둘 다** 실행된다. 실제 출력: `! 비정상 종료 2건` 바로 아래 `✓ 비정상 종료 없음`. PASS 카운터도 함께 증가하므로 헤더 "통과 67"에 유령 1건이 섞인다. 이 상쇄가 실제 신호를 덮었다 — `session-brief` **exit 1 ×2**(2026-08-24T09:22:46Z·09:22:58Z)가 로그에 있는데 **v2는 한 줄도 보고하지 않았다** | ③ 행동 체크포인트 | `if/else`로 바꾼다. 같은 형태를 전수 조사했다: `doctor.sh:411·412`, `guide-gate.sh:120·468·1018·1004·1671·1672`, `pack-smoke.sh:812`는 `$2`가 있거나 `ok`가 선행하므로 **안전**. 깨진 것은 1034 한 곳뿐이다 |
| E-07 | Suggestion | `templates/operating-contract.md`(예산) 대비 `skills/*/SKILL.md` frontmatter | 상주 비용의 71%(14,219자)가 스킬 description인데 예산이 없다. 예산이 있는 T0(2,425자)의 5.9배다. 스킬을 한 번도 호출하지 않아도 전량이 매 세션 상주한다 | ① 시작 브리핑 | 상위 항목부터 압축(scroll-stop-builder 916자 · seo-strategy 835 · scroll-stop-prompter 727 · marketing-workflow 710 · web-asset-generator 667). 임계값은 반복 증거가 쌓인 뒤에 |
| E-11 | Suggestion | `scripts/doctor.sh:1032` | `살아있는 훅: 5/6`의 분자는 로그의 **스크립트 이름** distinct이고 분모는 `hooks.json`의 **선언 엔트리 수**다. `handoff.sh`가 PreCompact·SessionEnd 두 이벤트에 걸려 있어 **분자는 구조적으로 5를 넘을 수 없다** → 영원히 5/6으로 미달을 표시한다. E-04와 같은 병(꺼지지 않는 경고)이 다른 자리에 있다 | ③ 행동 체크포인트 | 분모를 "고유 스크립트 수"로 바꾼다(`jq`로 command 경로를 `sort -u`). E-09 격리 후에야 이 지표가 의미를 갖는다 |

※ Critical 2건은 이 세션의 실행으로 재현됐다(E-08: 스킬 로드 경로·훅 출력, E-09: `--all` 전후 로그 증가).

## v2 판정 철회

| ID | 사유 |
| --- | --- |
| **E-06** (handoff 회전·상한 없음) | **오탐이다.** `scripts/handoff.sh:84`가 `ls -1t "$HD"/*.md \| tail -n +11 \| rm`으로 최근 10개만 유지한다. 실측 파일 수가 정확히 10개인 것은 상한이 **없어서**가 아니라 **작동 중이어서**다. 이 코드는 v2가 평가한 커밋(`bacaa9a`) 안에 이미 있었다(`git show HEAD:scripts/handoff.sh`로 확인) — v2는 파일 개수만 보고 코드를 열지 않았다. `--lessons`의 `[category: false-positive]` 첫 실물로 기록할 것 |

## 수축 제안

| 대상 | 사유 (과잉 or 중복 or 애매) | 대체 경로 | 회귀 위험 |
| --- | --- | --- | --- |
| `--usage`의 **훅 하트비트 + `살아있는 훅 N/M`** 지표 | **애매** — 데이터가 오염됐고(E-09) 분모는 도달 불가다(E-11). 두 조건 다 "출력이 행동으로 이어지지 않는다"에 해당한다. 지금 이 지표는 매 실행마다 미달을 표시하지만 아무도 무엇을 해야 하는지 모른다 | ①self-test 로그를 mktemp로 격리하고 ②분모를 고유 스크립트 수로 고친 뒤 **기준선을 다시 잡는다**. 격리가 어려우면 두 지표를 폐지한다 — 오염된 지표는 없는 지표보다 나쁘다 | 격리 후 이 저장소의 하트비트는 **0건**이 된다(설치본이 2.0.0이므로). 그것이 정직한 값이고, E-08을 드러내는 부수 효과가 있다 |
| 마케팅 계열 4스킬의 description | **과잉** — 상주 비용 상위 5개 중 4개가 마케팅 파이프라인이고 셋은 `marketing-workflow`가 오케스트레이션한다. 개별 트리거 문구가 중복. v2 제안 유지, 근거 불변 | 트리거 어휘를 `marketing-workflow`에 모으고 하위 3개는 짧은 지시로 축소 | 개별 호출(`/seo-strategy` 등) 트리거 정확도 하락 — 축소 후 호출 로그로 확인 필요하나, **그 로그가 지금 신뢰할 수 없다**(E-09). 격리 이후로 미룬다 |
| ~~`skills/security-review` vs 네이티브~~ | **철회** — 본문 대조 결과 중복 아님(아래) | — | — |

### P6-② 본문 대조 결과 (v2가 미룬 항목 — 이번에 수행)

| 커스텀 | 네이티브 | 판정 |
| --- | --- | --- |
| `security-review`(174줄) | `/security-review` | **중복 아님** — 전수 감사·의존성 CVE·시크릿 전수·결제 보안이 본문의 대부분. description이 "브랜치 diff에 새로 생긴 취약점만 보려면 내장을 쓰라"고 경계를 명시. `security-check` 훅과의 분업도 표로 선언(158~166행) |
| `skill-enhancer`(72줄) | `skill-creator` | **중복 아님** — 6패턴(경계 선언·행동 계약·실패 경로·기계 검증·생태계 배선·토큰 규율)만 다루고, 생성·테스트 프롬프트·A/B 벤치마크는 명시적으로 skill-creator 영역으로 넘긴다(12~14행) |
| `receiving-code-review`(148줄) | `/code-review` | **중복 아님** — 리뷰를 **생산**하는 게 아니라 **수신 후 사실 확인·반박**한다. 내장 `/code-review`의 산출을 입력으로 받는다고 description에 명시 |

> 네이티브의 존재는 중복의 증거이지 열등의 증거가 아니다 — 세 건 모두 **경계 선언이 본문에 실재**했다.
> v2가 grep으로 좁혔던 6파일 중 나머지 3건(`epcc-migrate`·`fix-issue`·`harness-evaluation`)은
> 네이티브 이름을 **문서로 언급**할 뿐 기능이 겹치지 않는다.

## 전회 대비 (v2)

- 결함: Critical **0→2** · Important 3→5 · Suggestion 2→2 (총 5→9)
- 신규: E-08 · E-09 · E-10 · E-11 · E-12 — **전부 계측기(doctor) 자신과 배포 경로에서 나왔다.**
  v1·v2가 "무엇을 재는가"를 개선하는 동안 "**재는 도구가 맞는가**"를 아무도 보지 않았다
- 철회: E-06 (오탐 — 전회 판정 비계승 규율이 실제로 작동한 첫 사례)
- 해소: 없음 (v2 이후 자산 커밋 0건)
- 상주 비용: 20,034자 → 20,034자 (변동 없음)
- 오탐 이력: 0건 → **1건** (`--lessons`의 `false-positive` 계측 시작 이후 첫 실물)
- **지표 변별력**: 이번 회차의 4건 중 3건(E-09·E-10·E-11)은 `doctor` 출력을 **믿지 않고 읽었을 때만**
  나온다. 기계 판정을 입력으로 받는 설계의 맹점이다 — 스킬은 "다시 세지 않는다"고 규정하지만
  **세는 방식은 봐야 한다**

## 미계측 항목 (정직 보고)

- **소비자 실사용 경로 비용 (P5)**: 이 저장소에는 소비자가 없다. `--usage --root <소비자>` 경로는
  준비됐으나 실행 대상이 없다. 상주 비용만 실측됐고 경로별 비용은 여전히 미계측
- **P6-① 무호출 31스킬의 과잉 판정**: v2는 "계측 기간이 짧아서"라고 적었으나, 실제 사유는
  **데이터 출처 무효**다(E-09). `skilluse.log`의 유일한 기록 `epcc-test-probe 1`도 픽스처다.
  격리 이후 실사용 로그가 쌓여야 판정 가능 — **기간 문제가 아니라 오염 문제로 재분류**
- **handoff 복원 충분성 (P2-③)**: PreCompact 산출물이 실제 복원에 충분한지는 컴팩트가 발생한
  세션에서만 검증 가능
- **`session-brief` exit 1 ×2의 근본 원인**: 2026-08-24T09:22Z 두 건이 어떤 입력에서 났는지는
  로그에 종료 코드만 남아 stderr가 없다. E-10 수정 후 재현 조건을 잡아야 한다
- **T1 규칙의 소비자 도달**: `--consumer`가 합성 캐시에서 8/8장 설치를 증명하지만,
  **실물 캐시가 2.0.0이고 3.18.0은 배포된 적이 없으므로**(E-08) 이 증명은 아직 실사용에
  적용된 적이 없다. 종결 조건은 4가지다 — ①캐시에 `3.18.0` 디렉토리 존재 ②`Skill()`의
  base dir이 3.18.0이고 내용이 7축 ③SessionStart 출력에서 "8번 + 10번 축" 문구 소멸
  ④소비자 `.claude/rules/`에 카드 8장 실재. **④가 CLAUDE.md가 "v2가 4개월간 놓친
  것이 정확히 이 한 가지"라고 못박은 항목이다**
