# 하네스(그래프) 평가 v1

- 날짜: 2026-08-19 · HEAD: 6556a70 · 플러그인: 3.1.0
- 시점 스탬프: Claude Code 2026-08 번들(/code-review·/security-review·/loop·skill-creator 실재 확인) · Opus 5 (1M) · 2026-08-19 — 네이티브/모델 과잉 판단 기준
- doctor: 통과 26+ · 경고 0 · 실패 0 (--all)
- 비고: **경로 단위 5축 체계의 첫 실행.** 구 10축 점수와 비교 불가 — 본 리포트가 기준선이다

## 축별 판정

| 축 | 측정 | 판정 |
| --- | --- | --- |
| P1 라우팅 일관성 | S/M/L 매치 잔존분은 취약점 심각도 등급표(health-check·security-review)와 평가 스킬 자신의 지시문뿐 | **충족** — 라우팅 문맥 위반 0건 (커밋 1에서 3건 해소) |
| P2 발동 정확성 | council-review·ai-review-loop 진입 계약 신설(그래프 선언+description) · epcc-reviewer 읽기 전용(disallowedTools) · 양성 픽스처 시크릿 주입 → exit 2 차단 확인 | **부분 충족** — ①② 충족. ③은 security-check 차단만 증명됨. handoff 복원 충분성은 미계측 (아래) |
| P3 루프 종료성 | 상한 보유 4파일: council(재질의 1회) · shared-workflow 사본 2(재검증 2회) · 평가 스킬(스탬프 30일) | **충족** — 무한 반복 선언 0건 (커밋 2에서 해소) |
| P4 되먹임 폐쇄성 | 저장소 3계 중 lessons만 승격 경로 보유(doctor --lessons → rule-promotion). insights·memory는 소비 노드 없음 | **미달** — E-01 |
| P5 경로 비용 | skilluse.log는 검증 프로브뿐. 클래스별 대표 시나리오 미실행 | **미계측** (아래) |

## 결함 목록 (심각도순)

| ID | 심각도 | 파일:행 | 실패 시나리오 | 수정 방향 |
| --- | --- | --- | --- | --- |
| E-01 | Important | skills/profile-update/SKILL.md · skills/insight-saver/SKILL.md | profile-update가 축적하는 듀얼 일치율(AI 독립 vs 프로파일)과 insight 문서를 읽는 노드가 그래프에 없다 → N세션이 지나도 판단 정확도가 규칙·스킬 diff로 반영되지 않는다. "같은 실수를 반복하지 않는다"는 목표가 lessons 1계에서만 작동 | insights·memory에 승격 경로 설계 — doctor --lessons의 집계 대상 확장 또는 rule-promotion 입력 확장. **차기 작업 후보 (이번 범위 밖)** |
| E-02 | Important | scripts/track-skill.sh:27 | 그래프가 instrumented:true로 선언한 build→track-skill을 스크립트가 방출하지 않음 → 스킬을 아무리 호출해도 '미실행 엣지' 경고가 영구 잔존 → 꺼지지 않는 경고는 무시를 학습시킴 | epcc_edge 추가 — **본 평가 첫 실행에서 발견, 커밋 6에서 수정·검증 완료** |
| E-03 | Suggestion | scripts/build-gate.sh:20 | 소스 무변경 세션에서 조기 종료 시 엣지 미방출 → 무변경 세션만 있는 프로젝트에서 동일한 영구 경고 | 매 실행 시 traversal 기록으로 변경 — **수정·검증 완료** |

## 전회 대비

- 첫 회 — 기준선. (구 10축 평가는 측정 체계가 달라 비교하지 않는다)
- 계획서(dev/active/graph-upgrade/plan.md) 기지 결함 대비: **B-1~B-5 전건 해소 확인** (doctor 실패 0)
- 지표 변별력: 첫 실행이 계획서에 없던 신규 결함 2건(E-02·E-03)을 검출 — **합격**

## 미계측 항목 (정직 보고)

- **P5 경로 비용**: 소비 프로젝트 실사용 graph.log·skilluse.log 부재. 이 저장소의 로그는 doctor 검증 프로브가 대부분이라 대표성이 없다. 차기 평가에서 실제 작업 세션 로그로 측정
- **handoff 복원 충분성 (P2-③)**: PreCompact 산출물이 실제 복원에 충분한지는 컴팩트 발생 세션에서만 검증 가능
- **플러그인 배포 도달성**: 사용자 캐시가 2.0.0 — 3.1.0 재설치 후 새 세션에서 v3 session-brief 출력 확인 필요
