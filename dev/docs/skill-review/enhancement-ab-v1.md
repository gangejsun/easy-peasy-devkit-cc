# skill-enhancer 실측 A/B v1 — insight-saver

- 날짜: 2026-08-20 · 방법: skill-creator 평가 루프 (스냅샷 baseline, 동일 프롬프트 3종 × 전/후 병렬 6실행, 채점 기준 실행 전 고정, 전 항목 기계 판정)

## 결과: 강화판 7/7 · 원본 5/7

| 평가 | 항목 | 강화판 | 원본 |
| --- | --- | --- | --- |
| E1 신규 저장 | 파일 1개 / 4섹션 구조 / 날짜 | ✅ / ✅ 4/4 / ✅ | ✅ / ❌ 0/4 (즉흥 구조) / ✅ |
| E2 중복 요청 | 신규 파일 0 / 기존 보존 | ✅ (Step 1.5 중단) / ✅ | ❌ 중복 생성 / ✅ |
| E3 lessons 경계 | insights 0 / lessons 카드 | ✅ / ✅ | ✅ / ✅ (동률·회귀 없음) |

## 판정이 갈린 지점 = 진단이 예측한 지점

- **P4 (저장 전 검증 게이트)**: 원본은 Step 3이 빈 제목이라 실행마다 구조가 다름.
  강화판은 4섹션 계약 + Step 4.5 grep 검증(실패 시 삭제)으로 구조 고정
- **P6 (만들지 않을 조건)**: 원본의 중복 방지는 "파일명 충돌 시 접미사" — 날짜가
  다르면 충돌이 없어 동일 주제 파일이 무한 증식. 강화판은 주제 grep으로 차단 후 병합 제안

## 부수 실측

- 토큰: E2에서 강화판 29.9k vs 원본 33.8k — 필요성 게이트의 조기 중단이 문서
  작성·저장·검증 비용을 제거. 총합도 강화판 94.3k < 원본 95.2k
- E3 동률: 기존에 잘 되던 경계 분기가 강화로 깨지지 않음 (회귀 없음)

## 결론

skill-enhancer의 6패턴 중 이번 대상에 적용된 P4·P6이 기계 판정 가능한 차이를
만들었고, 채점 기준은 결과 확인 전에 고정되었다. 강화 유지 확정.

---

## 갱신 (2026-09-08)

**대상 `insight-saver`는 그 뒤 삭제됐다.** 이 문서가 파이프라인의 **유일한** 실측 증거인데
가리키는 대상이 저장소에 없다 — 결론(P4·P6이 기계 판정 가능한 차이를 만든다)은 여전히
유효하지만, 살아있는 스킬로 재현된 적은 없다. `skills/` 26개 중 `evals/`를 가진 스킬은 0개다.

다음 A/B 후보: **`test-driven-development` 경계 선언 전/후.**
검증 질문 — 트리거 범위를 좁혀도 TDD 강제력이 유지되는가.
